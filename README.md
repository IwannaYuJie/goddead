# Goddead

Static landing page for [goddead.com](https://goddead.com).

## Current Experience

当前实现基线为 **v94「回敲邮局 / POST OFFICE OF RETURNED KNOCKS」**：全站共 205 个 hash 场景，静态资源缓存标记为 `v=94`。v94 在 v93 三项裁定集齐后开放 3 个新场景：三封只有孔的回敲信（门 / 代神席 / 来之前的你），在叩门间对着铜门环按节奏敲回去（原样 / 倒着 / 多敲一下，只看节奏比例，可听示范，空格键也能敲），回信寄到门外 / 代神席 / 痕迹室签收；三封信、三种方式都用过即开第一下仲裁庭。独立键 `goddead_v94_returned_knocks`。v91–v93 见 `docs/` 下对应设计文档。

完整玩家路线、支线网、v63-v94 解锁链与 v90–v94 闭环见 [`docs/GameplayFlow.md`](docs/GameplayFlow.md)。

The homepage is **The Living Shrine**, a hash-routed, scene-by-scene exploration game. Visitors knock three times at the sealed threshold — now a photographed bureau door — and on the third knock the door visually opens into a deep black-gold corridor before the visitor is pulled through into the next room; each completed action thereafter advances the ritual automatically.

The flow is auto-advancing: three knocks open the door and lead into the **Visitor Protocol**. Rule one reaches the **Scripture Corridor**; rules two through four and seven enter the v31 forecourt weave; rule five enters the return audit, rule six the midnight callback, and rule eight the proxy-admission window. Reading three crooked fragments reveals the **Third Night-Watch Room**; covering the 05:02 log entry and attempting sign-out unlock the **Echo Switchboard**; listening to the first three callback lines and connecting the fourth opens the **Dead Letter Office**; archiving three returns and signing the blank receipt spawn the **Divine Name Cancellation Office**; searching `GODDEAD` and refusing cancellation rewrite the refusal as an appointment at the **Acting Deity Desk**; pushing the presence switch to 100% opens the **Offering** furnace; offering a non-empty prayer ignites the incinerator and auto-advances into **The Sacred Reliquary Vault**; auditing three remnants and stamping the final seal advances the visitor into **Remembrance**, where the page records what it remembers. Remembrance then acts as the hub for the v28 endings and the sequential v63-v90 post-ending chapters. A hidden **Ninth Rule** remains accessible through the protocol anomaly and does not auto-cycle.

Three optional branch rooms (v29) hang off the corridor's fragments. The first active click on the 回声 fragment opens the **Echo Archive**, the 血管 fragment the **Vein Maintenance Well**, and the 忏悔 fragment the **Confession Weighing Room** — each after a ~0.7–1.0 s feedback beat (~0.3 s under reduced-motion), with the main-line auto-advance cancelled in the branch's favour and no second continue button anywhere. Every room holds three focusable hotspots: the archive's receivers return to the threshold or the corridor, and the 03:17 bell transfers deeper into the **Distortion Transfer Chamber**; the well's valves run downstream to the corridor, upstream to the protocol, or open the **Reverse-Flow Pump Room** behind the isolation valve; the weighing room's pans confess into the protocol or the corridor, while refusing confession is filed into the **Nameless Ledger Vault**. Branches are always optional, never a hard gate on the main line; visited rooms gain re-entry buttons in the corridor and directory entries that survive reloads, all recorded in a fault-tolerant `goddead_v29_branches` state that never touches main-line progress. Remembrance gains a single branch-memory line — still eight stat cards.

The three deep rooms (v30) form a loop under the branches, each entered from its parent room and each holding three more focusable actions with auto-advance. The transfer chamber relays distorted voices into the vein network, seals your own voice into a wax cylinder (back to the protocol), or rings 03:17 again — through to the night-watch room only once the watch is unlocked, otherwise back to the corridor. The pump room vents echo pressure back to the transfer chamber, drains black sediment into the ledger pipes, or climbs the emergency ladder to the watch (falling back to the protocol when the watch is still locked). The ledger vault crosses out your own name (into the transfer chamber), files you as a still-present witness (into the pump room), or rejects the entire record — normally back to the protocol, but once all three deep rooms are visited the rejection lands in the corridor or the watch by unlock state. Deep visits persist at click time in a fault-tolerant `goddead_v30_branch_depth` state that never touches v29, v28, or main-line progress; visited deep rooms gain corridor re-entry buttons and directory entries, direct-hash access to an unvisited deep room falls back to its parent branch, and Remembrance adds one deep-branch memory line — still eight stat cards.

The forecourt weave (v31) turns the threshold and the protocol into real branching. Three native-button hotspots sit on the bureau door itself — the black eclipse above the lintel, the uncountable symbols on the left wall, and the "come back" traces on the right — each discoverable on a clean save and keyboard-operable, each persisting its visit at click time and auto-advancing after a ~0.7–1.0 s beat (~0.3 s under reduced-motion) without ever covering the door's three-knock target. They lead into three new forecourt scenes: the **Inverted Peephole**, the **Missing-Numeral Niche**, and the **Returning Passage**, each holding three focusable actions whose nine destinations close a triangle among themselves, the threshold, the protocol, and the corridor — with no second continue button anywhere. The protocol now truly diverges: rule two leads to the Returning Passage, rules three and seven to the Niche, rule four to the Peephole, while rules one, five, six, and eight still walk the main line into the corridor, and the anomalous 玖 count keeps its priority into the hidden **Ninth Rule**. The first accepted activation locks its transition both at the door (a scheduled hotspot detour ignores later knocks, and a completed third knock ignores later hotspots) and inside the protocol (any other rule input is ignored while a transition pends), so 其二-then-其一 still lands in the Returning Passage and 其一-then-其二 still lands in the corridor. All of it lives in a fault-tolerant `goddead_v31_forecourt_weave` state (visited flags, a nine-mark whitelist, last choice, and a clamped transition counter) that never reads or writes v28/v29/v30 or main-line progress; rapid clicks and repeated Enter/Space count once and schedule once, mid-feedback reloads never double-count, and leaving a scene cancels its pending timer. The three scenes have no unlock gate — clean saves may enter from the door or by direct hash — visited rooms restore directory entries (`01α / 窥孔`, `01β / 失号龛`, `01γ / 回返夹道`), and Remembrance adds a single forecourt line — still eight stat cards.

The inner annex (v32) extends each forecourt room one level inward instead of stacking more content onto the ending. Exactly three v31 actions are rewired — closing the peephole's eye now leads to the **Archive of Closed Eyes**, taking the blank numeral plate to the **Unnumbered Vestibule**, and walking the passage backward to the **Reverse Stairwell** — while every other v31 action, the protocol detours, and the 玖 anomaly stay untouched. The three annex rooms form their own triangle (the archive's sealed sight leads to the vestibule, the vestibule's tenth door to the stairwell, the stairwell's upward descent back to the archive) and keep exits to the threshold, the protocol, and the corridor. Everything follows the same contract as v31: three visible native-button actions per room with click/Enter/Space, first-locked auto-advance after a short feedback beat, no continue buttons, direct-hash access that only records the visit, directory entries (`01δ / 闭目档案`, `01ε / 无号前厅`, `01ζ / 逆向阶井`) restored after first visit, a fault-tolerant `goddead_v32_inner_annex` state that never touches v28–v31 or the main line, and one annex memory line in Remembrance — still eight stat cards.

The anomaly review (v33) turns the annex into a repeatable, scored side line. Each of the three v32 annex rooms gains a clearly visible fourth entry action — the original nine actions keep their exact wording, marks, feedback, and destinations — that files the room's anomaly with the **Office of Anomaly Review** (`#anomaly-review`). Each run presents exactly three case files (always one `baseline` plus two distinct anomalies, drawn from a deterministic entry+cycle rotation matrix, never bare randomness) and exactly two native buttons — 登记为异常 / 维持原案. The first accepted judgement locks the round, shows immediate feedback, updates the in-world review slip (复核值 / 连续复核 / 误报 / 最佳连续), and auto-advances to the next file (~0.7–1.0 s, ~0.3 s under reduced-motion) with no second continue button; the third judgement settles once and routes by score: 3 correct into the **Evidence Preservation Vault** (`#evidence-vault`), 2 correct back to the entry annex with a one-time "已复核" note, 0–1 correct into the **False-Positive Shaft** (`#false-positive-shaft`). Both result rooms hold three first-locked actions that wire back into the forecourt network — the vault seals, returns, or exits toward the corridor, the shaft can restart a rotated cycle, append to the protocol, or walk back to the threshold — with no new endings. Mid-run reloads restore the current round and score without double-counting, leaving a scene clears every pending timer, direct-hash entry shows the neutral first case and never auto-decides, and result rooms normalize back to the review office unless the current outcome or a past visit admits them. Everything lives in a fault-tolerant `goddead_v33_anomaly_review` state that never reads or writes v28–v32 or main-line progress; visited rooms restore directory entries (`01η / 异常复核`, `01θ / 异常保全`, `01ι / 误报回收`), and Remembrance adds a single review-memory line — still eight stat cards.

The unclaimed valuation (v34) extends both v33 result rooms one level deeper. The **Evidence Preservation Vault** and the **False-Positive Shaft** each gain a clearly visible fourth entry action — the original six actions keep their exact wording, marks, feedback, and destinations — that ships unclaimed evidence to the **Unclaimed Valuation Room** (`#unclaimed-valuation`). Each batch presents three relic cards (deterministic entry+cycle batches from a four-row whitelist matrix, never bare randomness); valuing is click-to-execute with no confirmation page — every relic adds to 本批估值 while consuming 封印完整, and each relic carries its own combo effect (the thirteenth clock shields the next relic's damage once, the tooth key grows with valuation order, the hollow idol pays extra as the third pick, the black wax lung boosts not-yet-valued relics, the unopened eye reveals the remaining exact numbers, the receipt bone pays double from the shaft). After any valuation the 盖章结算 stamp becomes available as an active stop-loss — never a continue button: meeting 本批定额 rides the **Quota Elevator** (`#quota-elevator`, three first-locked exits to the vestibule, the return passage, or the corridor), falling short returns to the return passage with a one-time under-quota note, and a broken seal drops straight back into the false-positive shaft with a one-time breach note. All three relics valued with the seal intact auto-settles without a stamp click. Mid-batch reloads restore the batch, the valuation order, and the computed ledger without recomputing effects or double-counting; each batch completes exactly once; the elevator normalizes back to the valuation room unless the current outcome or a past visit admits it. Everything lives in a fault-tolerant `goddead_v34_unclaimed_valuation` state that never reads or writes v28–v33 or main-line progress; visited rooms restore directory entries (`01κ / 无主估值`, `01λ / 定额电梯`), and Remembrance adds a single valuation-memory line — still eight stat cards.

The unnumbered floor (v35) turns the network sideways instead of longer. The **Unnumbered Vestibule** gains a fifth action and the **Quota Elevator** a fourth — every existing action keeps its exact wording, feedback, mark, destination, and first-lock behavior — both leading into the **Unnumbered Floor** (`#unnumbered-floor`), a free-order hub whose three doors lead to the **Bell-less Ward**, the **Seeping Records**, and the **Reverse Laundry**. Each duty room holds two shift actions (signal/door-debt yields, each settling that room once per cycle) and one yield-less crossing, so the three rooms interlink without forcing a return to the hub; completed rooms stay visitable with their duty buttons disabled. Finishing any two rooms unseals the hub's missing-floor elevator, which settles once per cycle: `routeScore = max(0, signal − debt)` rides at ≥4 back up to the quota elevator, hands 2–3 over to the anomaly review as a fresh neutral run, and at ≤1 drops the visitor back into the return passage — each tier shows its one-time feedback at the landing scene, no new endings anywhere. Everything is click-to-execute with a short feedback beat and auto-advance; reloads restore the current cycle without re-settling, leaving a scene clears its timers, and the elevator guard admits the v35 high-signal ride through one narrow outcome exception only. Everything lives in a fault-tolerant `goddead_v35_unnumbered_floor` state that never reads or writes v28–v34 or main-line progress; visited rooms restore directory entries (`01μ / 无号层`, `01ν / 无铃病房`, `01ξ / 渗水档案`, `01ο / 逆照洗衣`), and Remembrance adds a single floor-duty memory line — still eight stat cards.

The night-shift registry (v36) fixes v35's lopsided math with one more lateral link instead of a longer chain. The **Return Passage** gains a fourth action and the **Unnumbered Floor** a fourth door — the night registry window — both leading into the **Night-Shift Registry** (`#night-shift-registry`), an unmanned counter where three permit trays keep moving. Once per floor cycle the visitor may claim exactly one duty permit: the **Mute Bell** (+2 signal), the **Blank Name** (+2 door-debt), or the **Reverse Badge** (+4 door-debt) — the claim lands immediately in the running tally, shows its feedback, and auto-returns to the hub with no confirmation page and no continue button; a claimed permit stays visible, pressed, and disabled until the next cycle. Permits make every settlement tier reachable through normal play: no permit still rides high at 4, the mute bell lifts a two-room run to high 6, the blank name dips it to review 2 (handed to the anomaly review as a fresh neutral run), and the reverse badge sinks it to debt 0 (back into the return passage). Entering the registry from a settled run starts a fresh cycle first; reloads keep the claimed permit without re-applying it; the per-cycle theoretical caps grow to 12 signal / 8 debt. Everything extends the same fault-tolerant `goddead_v35_unnumbered_floor` state — old saves gain safe defaults with no migration — and never reads or writes v28–v34 or main-line progress; visiting the registry restores the directory entry (`01π / 夜班登记`), and Remembrance adds a single registry memory line — still eight stat cards.

The midnight callback (v37) turns protocol rule six into a real early branch. Rule six keeps its exact wording and feedback — only its destination changes, from the corridor to the **Midnight Callback Desk** (`#midnight-callback`) — and the **Return Passage** gains a fifth action for the same desk; every other rule, the ninth-rule anomaly, and the passage's four existing actions stay untouched. The desk holds three night lines to the v35 duty rooms. Connecting a line only writes the pending mark — the visitor is sent to the room itself, where a new fourth action reports back (ward +3/0, records +2/+1, laundry +1/+2 verification/line-debt), sharing the room's existing first-lock while leaving every v35 duty value, mark, and settlement field untouched. Two reports auto-settle with no settle button: ward+records rides clear to the anomaly review, ward+laundry falls uncertain into the inverted peephole, and records+laundry sinks contaminated into the return passage. Pending survives detours through a room's original actions, reloads restore pending and completed lines without re-applying values, and a settled desk restarts as a fresh cycle on re-entry. Everything lives in a fault-tolerant `goddead_v37_midnight_callback` state that never reads or writes v28–v36 or main-line progress; visiting the desk restores the directory entry (`01ρ / 午夜回拨`), and Remembrance adds a single callback memory line — still eight stat cards.

The proxy admission window (v38) returns to the door itself. A fourth door hotspot — discoverable on a clean save, keyboard-operable, sharing the threshold first-lock with the three knocks and the v31 hotspots — plus protocol rule eight (exact wording and feedback preserved, only its destination changes) both lead into the **Proxy Admission Window** (`#proxy-admission`), where the institution behind the door deputizes the visitor to review three night callers per cycle from a deterministic four-round roster. Each caller can be questioned (register name, shadow, knock — testimony only, never scored) and then judged once: 准许入内 or 留在门外. The first accepted judgement locks the caller, shows its verbatim feedback, and auto-advances; the third auto-settles with no next/confirm/continue button. Two correct answers build 在场证 (+2 each), a wrong admit of an echo marks admittedEcho, a wrong refuse of the present marks rejectedPresent: all three right verifies the visitor back into the protocol, a rejected presenter drops into the inverted peephole, an admitted echo into the return passage, and two or more debts into the glyph niche. Proof, debt, error flags, settled and the outcome are always recomputed from the stored decisions — and only decisions that form the current roster's contiguous judged prefix count, so forged saves repair themselves; reloads resume the exact visitor without re-judging, leaving mid-beat clears timers, and a settled window opens the next deterministic cycle with history kept. Everything lives in a fault-tolerant `goddead_v38_proxy_admission` state that never reads or writes v28–v37 or main-line progress; visiting the window restores the directory entry (`01σ / 代审窗`), and Remembrance adds a single admission memory line — still eight stat cards.

The route-of-return audit (v39) turns protocol rule five into a real early branch. Rule five keeps its exact wording and feedback — only its destination changes, from the corridor to the **Route-of-Return Audit** (`#return-audit`) — a three-way hub where the institution makes anyone claiming to remember the way back re-walk two of three routes, in any order: the **Echo Turn** (`#echo-turn`), the **Vein Turnstile** (`#vein-turnstile`), and the **Confession Locker** (`#confession-locker`). Each route holds exactly two judgements with verbatim immediate feedback; the first finished route auto-returns to the hub without pre-selecting the second, and the second keeps its route feedback for one beat, swaps to a settlement feedback back at the hub, then auto-advances — no next/confirm/continue button anywhere. Two recalls verify the visitor back into the protocol; two missteps lose them into the return passage; a 1/1 split lands in the existing branch that was failed (echo archive, vein maintenance well, or confession weighing room, admitted through a narrow guard exception). Completed routes lock disabled+pressed, the third route waits for the next cycle, and order/decisions only count as a contiguous legal prefix — recall, misstep, wrong route, settled and the outcome are always re-derived, so forged saves repair themselves; a judgement is only accepted from its own live scene with a legal pending route, so programmatic off-route clicks can never advance the run; reloads resume the pending route, the completed list, an in-flight settlement — and even a reload inside a judgement's feedback beat replays the verbatim feedback and re-arms the return or settlement exactly once — while leaving mid-beat clears timers. Everything lives in a fault-tolerant `goddead_v39_return_audit` state that never reads or writes v28–v38 or main-line progress; visiting the station restores the directory entry (`01τ / 归路核验`), and Remembrance adds a single audit memory line — still eight stat cards.

The lateral corridors (v40) deepen the very first scene instead of extending the chain. A new environmental layer (`assets/threshold-lateral-hall.webp`) turns the flat black around the door into two receding side corridors, the central closed/open door imagery grows ~12% with its open state untouched, and two new space hotspots — **左廊** (`#hotspot-lateral-left`) and **右廊** (`#hotspot-lateral-right`) — join the eclipse/glyph/return/proxy hotspots and the three knocks under the same threshold first-lock. They lead to three real rooms: the **Unlit Lamp Gallery** (`#unlit-lamp-gallery`), the **Borrowed Shadow Gallery** (`#borrowed-shadow-gallery`), and the **Hinge Sorting Room** (`#hinge-sorting-room`), each carrying three in-picture hotspots over real objects — never a bottom card list. Every one of the nine actions gives verbatim feedback and auto-advances, forming a walkable spatial network that genuinely lands in v31's return passage, peephole chamber and glyph niche (admitted only through a narrow v40 lastAction exception in the new forecourt guard — unvisited direct access now falls back to the threshold, while every pre-existing path is read-only exempted), in v38's proxy window, and in v39's audit station. The first accepted action locks the scene's hotspots for the beat — and only answers on its own live scene, so scripted clicks on hidden DOM can never advance state or arm timers; a strict four-field pending (`scene/action/target/feedback`, any mismatch dropped) replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat, and rival clicks in one beat count once. Everything lives in a fault-tolerant `goddead_v40_lateral_corridors` state that never reads or writes v28–v39 or main-line progress; visiting restores the directory entries (`01υ / 无灯灯廊`, `01φ / 借影陈列`, `01χ / 铰链分拣`), and Remembrance adds a single corridor memory line — still eight stat cards.

The protocol backrooms (v41) turn the second scene's bulletin board itself into an explorable mechanism. The original `assets/visitor-protocol-board.webp` stays untouched; three native hotspots now sit on its real red-thread knots, its blank nameplate and its brass bell button — **红线** (`#protocol-hotspot-thread`), **空名** (`#protocol-hotspot-nameplate`), **无舌铃** (`#protocol-hotspot-bell`) — sharing the protocol first-lock with all eight rules (first accepted rule or hotspot wins the beat, tested both ways) while the 捌/玖 anomaly and the `#ninth` path keep their exact semantics. Each entry writes its verbatim feedback into an independent `#protocol-backroom-response` pinned visibly under the board — never reusing the rules' toast — and auto-advances to a real room: the **Red-Thread Registry** (`#red-thread-registry`), the **Cloakroom of Blank Names** (`#blank-name-cloakroom`), and the **Clapperless Bell Desk** (`#clapperless-bell-desk`). Each room carries three in-picture hotspots over real objects (spool/seal/gap, coat/hook/token, bell/tube/cord); the nine actions auto-walk the triangle and genuinely land in the corridor, the return audit, the proxy window, the midnight callback and back at the protocol. Every handler — board entries and child actions alike — validates the live scene before any state access, feedback or timer, so scripted clicks on hidden DOM do nothing; a strict four-field pending replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat, and rival clicks in one beat count once. Everything lives in a fault-tolerant `goddead_v41_protocol_backrooms` state that never reads or writes v28–v40 or main-line progress; visiting restores the directory entries (`01ψ / 红线登记`, `01ω / 空名寄存`, `02α / 无舌铃台`), and Remembrance adds a single backroom memory line — still eight stat cards.

The protocol drift (v42) makes the same bulletin board drift on revisit. Once any two of the three v41 backrooms are seen (read-only — v42 never writes the v41 key), a fourth entry **巡查** (`#protocol-hotspot-drift`) appears over the board's old wax seal, sharing the protocol first-lock with the eight rules and the three v41 entries. The inspection scene (`#protocol-drift`) shows the untouched baseline board or one of three locally-mutated variants (a ninth thread knot, a slid-open nameplate with a hanging token, a weeping clapperless bell mouth) along a fixed staggered sequence `["thread","normal","bell","name","thread","bell","normal","name"]` starting at `(cycle * 3) % 8`. Four in-picture judgement hotspots — report thread, report nameplate, report bell, or press the wax seal for "no drift" — give verbatim feedback and auto-advance: a correct answer swaps to the next board in-place (focus returns to the first hotspot), three in a row completes the cycle with a completion beat back to the protocol or into the matching backroom, and any misjudgement (a misreport on the clean board, a missed real anomaly, or blaming the wrong spot — the feedback names both the choice and the real anomaly) resets the streak and sends the visitor into the corresponding v41 room for real. Inspections, correct answers, misreports, current and best streaks accumulate per run; the same beat accepts only the first input, and every handler validates the live scene before any state access, feedback or timer. A strict seven-field pending (`round/answer/correct/target/feedback/nextCycle/nextCursor`, validated against the fixed rule table field-by-field) replays the feedback and finishes the action after a reload or a cancelled beat without ever re-counting; completed cycles reset the streak on landing. Everything lives in a fault-tolerant `goddead_v42_protocol_drift` state that never reads or writes v28–v41 or main-line progress; visiting restores the directory entry (`02β / 守则漂移`), and Remembrance adds a single drift memory line — still eight stat cards.

The counter-knock network (v43) goes back to the first minute on the doorstep. After the first accepted knock — and still after the second, more urgent — three brief counter-knock hotspots appear inside the door itself: **左响** (`#hotspot-counter-knock-left`), **缝响** (`#hotspot-counter-knock-seam`, only on the lower seam so the door's knock zone stays free), and **右响** (`#hotspot-counter-knock-right`). The third knock hides them and keeps the untouched open-door flow into the protocol; they share the threshold first-lock with the three knocks and every v31/v38/v40 hotspot. Each counter-knock writes its verbatim feedback and detours to a real room: the **Counter-Knock Gallery** (`#counter-knock-gallery`), the **Unanswered Vestibule** (`#unanswered-vestibule`), and the **Under-Sill Dispatch** (`#undersill-dispatch`). The three rooms interlink and genuinely land in the protocol, the inverted peephole, the return passage, the glyph niche, the proxy window and the hinge sorting room — the v31 forecourt guard admits only the narrow `lastAction` exception of the current run, every other unvisited direct arrival still falls back to the threshold. Each room carries three in-picture hotspots over real objects (inward/still/shadow, first/second/third, seal/blank/hinge) with verbatim feedback; the first accepted action locks the scene for the beat, handlers validate the live scene before any state access, and a strict four-field pending replays the feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v43_counter_knock` state that never reads or writes v28–v42 or main-line progress; visiting restores the directory entries (`02γ / 回敲廊`, `02δ / 未应门`, `02ε / 门槛下`), and Remembrance adds a single counter-knock memory line — still eight stat cards.

The spaces behind the pages (v44) turn three corridor fragments into real detours. The eight fragments keep their exact texts, counting, read styles and the three-fragment watch threshold; `f2/f3/f4` keep their v29 routing untouched. Only the first-ever read of `f1` (the lagging shadow), `f5` (ash shaped like a door) and `f8` (the act of being left behind) now also detours — after the original counting — to the **Lagging-Shadow Cloister** (`#lagging-shadow-cloister`), the **Ash-Door Foundry** (`#ash-door-foundry`), and the **Retention Vault** (`#retention-vault`), sharing the corridor AutoAdvance scope with a detour first-lock: one counting, one scheduling per beat, the first accepted fragment detour wins even when it is the third fragment (never both watch and detour), and an already-read fragment never re-counts or re-detours. Each new room carries three in-picture hotspots over real objects (pin/outline/catch, bellows/key/door, tag/imprint/page) with verbatim feedback, forming a loop that genuinely lands in the borrowed shadow gallery, the return passage (admitted only by the narrow `lastScene=shadow && lastAction=catch` exception in the v31 guard — clean direct arrivals and mismatched states still fall back to the threshold), the protocol, the unlit lamp gallery and the counter-knock gallery. Handlers validate the live scene before any state access, feedback or timer; the first accepted action locks the scene for the beat; a strict four-field pending replays the feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v44_paperback_spaces` state that never reads or writes v28–v43 or main-line progress — the fragment entry itself only causes the original counting change it always caused; visiting restores the directory entries (`02ζ / 滞影`, `02η / 灰门`, `02θ / 留置`), and Remembrance adds a single paperback memory line — still eight stat cards.

The absent relief (v45) turns the third night-watch room's three narrative props into real side-line entries. Three native-button hotspots now sit on the room's real objects — the frozen 03:17 clock arbor, the dead lamp, and the empty chair — each discoverable before the fourth line is unlocked, each writing its verbatim feedback and auto-advancing into one of three new rooms: the **Minute-Before Archive** (`#minute-before-archive`), the **Cold-Wick Service Bay** (`#cold-wick-service-bay`), and the **Absent Relief Locker** (`#absent-relief-locker`). Each room carries three in-picture hotspots over real objects (hand/card/shaft, wick/cable/fuse, coat/chair/roster) with verbatim feedback, and the nine actions genuinely land in the lagging-shadow cloister, the night-shift registry, each other, the unlit lamp gallery, the counter-knock gallery, the proxy admission window, the borrowed shadow gallery, and back at the watch itself. The entries share the watch's first-destination lock with the existing fourth-line transition both ways: an already-armed switchboard transition makes the entries inert, and an accepted entry clears the watch scope so the main line can never rewrite its destination. The entries never write `goddead_watch` / `goddead_line4`, never cover the 05:02 entry, and never sign out — covering 05:02 plus one sign-out attempt remains the only way to unlock the fourth line and its original switchboard path. Handlers validate the live scene before any state access, feedback or timer; the first accepted input locks the beat and rival clicks count once; a strict four-field pending replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v45_absent_relief` state that never reads or writes v28–v44 or main-line progress; visiting restores the directory entries (`02ι / 分前`, `02κ / 冷芯`, `02λ / 缺班`), and Remembrance adds a single relief memory line — still eight stat cards.

The side tones (v46) turn the echo switchboard's own props into a second side loop. Three native-button hotspots now sit on the board's real objects — the dead receiver at the lower left, the loose unnumbered plug in the foreground, and the still-lit red return lamp — each writing its verbatim feedback into an independent `#sidetone-entry-response` under the figure and auto-advancing into one of three new rooms: the **Unseated Listening Booth** (`#unseated-listening-booth`), the **Unnumbered Jack Field** (`#unnumbered-jack-field`), and the **Return-Ring Morgue** (`#return-ring-morgue`). Each room carries three in-picture hotspots over real objects (earpiece/mouthpiece/chair, plug/socket/tag, lamp/slip/bell) with verbatim feedback, and the nine actions genuinely land in the midnight callback desk, the counter-knock gallery, each other, the red-thread registry, the night-shift registry, the unlit lamp gallery, and the unanswered vestibule. The entries share the switchboard's first-destination lock with the fourth-line transition both ways: an already-scheduled deadletter transition makes the entries completely inert, and an accepted entry arms `switchSidetoneArmed` so none of the four patch buttons can write `goddead_line4` late or rewrite the destination — hearing the first three lines and connecting the fourth still works exactly as before, and the original `#deadletter` path is untouched. Handlers validate the live scene before any state access, feedback or timer; the first accepted input locks the scene's three hotspots for the beat and rival clicks count once; a strict four-field pending replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v46_sidetones` state that never reads or writes v28–v45 or main-line progress; visiting restores the directory entries (`02μ / 失席`, `02ν / 无号孔`, `02ξ / 回铃`), and Remembrance adds a single side-tone memory line — still eight stat cards.

The returned rooms (v47) turn the dead letter office's own furniture into the next side loop. Three native-button hotspots now sit on the office's real objects — the three brass pneumatic tubes on the back wall, the return-letter cabinet at the upper right, and the blank receipt on the desk — each writing its verbatim feedback into an independent `#return-room-entry-response` under the figure and auto-advancing into one of three new rooms: the **Unclaimed Pneumatic Intake** (`#unclaimed-pneumatic-intake`), the **Returned-Address Cabinet** (`#returned-address-cabinet`), and the **Blank Receipt Press** (`#blank-receipt-press`). Each room carries three in-picture hotspots over real objects (carrier/horn/cord, envelope/wheel/drawer, sheet/ink/lever) with verbatim feedback; the nine actions close a walkable loop among the three rooms and genuinely land in the midnight callback desk, the return-ring morgue, the red-thread registry, the retention vault, the unclaimed valuation room, and the minute-before archive. The entries share the dead letter office's first-destination lock with the blank-receipt main line both ways: an already-scheduled cancellation transition makes the entries completely inert, and an accepted entry arms `deadletterReturnRoomArmed` so neither the three return buttons nor the receipt button can write `goddead_deadletter` late, reveal registry text, unlock the receipt, sign it, or rewrite the destination — filing three returns and signing the blank receipt still works exactly as before, and the original `#cancellation` path is untouched. Handlers validate the live scene before any state access, feedback or timer; the first accepted input locks the scene's three hotspots for the beat and rival clicks count once; a strict four-field pending replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v47_returned_rooms` state that never reads or writes v28–v46 or main-line progress; visiting restores the directory entries (`02ο / 气送井`, `02π / 退址柜`, `02ρ / 压印台`), and Remembrance adds a single returned-rooms memory line — still eight stat cards.

The cancellation copies (v48) turn the cancellation office's own hardware into the next side loop. Three native-button hotspots now sit on the terminal's real objects — the always-blank search screen, the still-lit red confirmation lamp, and the stacked file trays on the desk — each writing its verbatim feedback into an independent `#cancellation-copy-entry-response` under the figure and auto-advancing into one of three new rooms: the **Blank-Screen Underarchive** (`#blank-screen-underarchive`), the **False-Confirmation Desk** (`#false-confirmation-desk`), and the **Witness Carbon Archive** (`#witness-carbon-archive`). Each room carries three in-picture hotspots over real objects (screen/paper/aperture, lamp/seal/chute, sheet/drawer/card) with verbatim feedback; the nine actions close a walkable loop among the three rooms and genuinely land in the returned-address cabinet, the unseated listening booth, the counter-knock gallery, the unclaimed pneumatic intake, the red-thread registry, and the absent relief locker. The entries share the cancellation office's first-destination lock with the refusal main line both ways: an already-scheduled acting-deity transition makes the entries completely inert, and an accepted entry arms `cancellationCopyArmed` so neither the search form nor the refuse button can write `goddead_cancellation` late, add queries, hit the archive, reveal records, write refused, or rewrite the destination — searching `GODDEAD`, reading the five-line archive and refusing cancellation still works exactly as before, and the original `#acting` path is untouched; searches alone never schedule a destination, so the entries stay available after wrong or right queries and `enterCancel()` restores the persisted state on return. Handlers validate the live scene before any state access, feedback or timer; the first accepted input locks the scene's three hotspots for the beat and rival clicks count once; a strict four-field pending replays the verbatim feedback and re-arms the transition after a reload or a cancelled beat without re-counting. Everything lives in a fault-tolerant `goddead_v48_cancellation_copies` state that never reads or writes v28–v47 or main-line progress; visiting restores the directory entries (`02σ / 屏底库`, `02τ / 误准灯`, `02υ / 复写库`), and Remembrance adds a single cancellation-copy memory line — still eight stat cards.

The tactile forecourt (v49) adds no rooms: it reworks the three v31 forecourt scenes so every action lives inside the picture. All eleven existing buttons — the peephole chamber's mirror/listening-tube/eyelid, the glyph niche's tally marks/scraped seventh plate/blank porcelain plate, and the return passage's footprints/door ring/far door/permit slot/wall telephone — are now native-button hotspots pressed onto the real objects of three new bitmaps (`assets/forecourt-peephole-tactile.webp`, `assets/forecourt-glyph-niche-tactile.webp`, `assets/forecourt-return-passage-tactile.webp`), each stage holding a stable 3:2 ratio so desktop, short-desktop and mobile share one set of coordinates. Every button keeps its original id, accessible text, `aria-pressed` semantics, verbatim feedback, state writes, target hash, AutoAdvance behavior and first-lock; the old below-figure `.branch-choices` card containers are deleted outright with no hidden copies, and no localStorage key, route, room, continue button or second confirmation is added.

The tactile annex (v50) adds no rooms either: it reworks the three v32 annex scenes the same way. All thirteen existing buttons — the archive's half-open drawer/listening box/cut floor shadow/sealed evidence bag, the vestibule's hanging blank porcelain plate/open ledger with ink pad/handle-less black door/review drop slot/dim red basement button, and the stairwell's upward flight/zeroth-step seam/narrow black mirror/brass report box — are now native-button hotspots pressed onto the real objects of three new bitmaps (`assets/annex-eyelid-tactile.webp`, `assets/annex-vestibule-tactile.webp`, `assets/annex-stairwell-tactile.webp`), reusing the v49 stable 3:2 tactile stage and hotspot styling so desktop, short-desktop and mobile share one set of measured coordinates. Every button keeps its original id, accessible text, `aria-pressed` semantics, verbatim feedback, state writes, target hash, AutoAdvance behavior, first-lock and rival semantics; the old below-figure `.branch-choices` card containers are deleted outright with no hidden copies. On 390px each hotspot shows an `aria-hidden` one-to-two-line short visual label while the full title stays in the DOM and the accessibility tree, and mobile focus never expands the long hint. No localStorage key, route, room, continue button or second confirmation is added.

The annex three debts (v51) lets the thirteen existing annex actions teach the rooms how to answer. Every accepted action adds exactly one tick to one of three leanings — 见证 (witness, 5 actions: listening, reviewing, printing, looking back, reporting), 失号 (unnumbered, 4: searching, filing, the tenth door, the unnumbered exit) and 逆行 (reverse, 4: submitting, the basement button, climbing, the zeroth step) — capped at 9 each in a new fault-tolerant `goddead_v51_annex_debts` state that never reads or writes v32/v33/v35 or the main line. A small brass-stamped tally plate on the figure's upper-right edge of each annex room shows all three rows as zero-to-nine independent tick elements with exact aria-labels (a scene object, not a global HUD; no text glyphs); each tick lands with a faint brass stamp sound and an aria-live announcement like 「见证留下第一道刻痕」, while reduced-motion drops the plate stir and the anomaly breathing. When any leaning reaches 3, the matching room atomically swaps its figure to a mutated variant and its threshold hotspot appears as an additional branch — the archive's three eyelid drawers open into pale eyes (让它们看清, onward to the vestibule), the vestibule's handle-less door grows a narrower, deeper tenth frame (进入未编号的第十门, onward to the stairwell), and the stairwell's mirror fills with an impossibly reversed copy of the stairwell (走进倒置的阶井, onward to the archive). All thirteen original hotspots stay visible, focusable and clickable in the same beat — nothing is ever hidden or replaced; the three co-located originals keep the outer frame band (the drawer cabinet's knob column, the door's lintel, the mirror's left frame) while each anomaly hotspot covers only the inner layer, so the pair never overlaps or swallows a click on desktop or mobile. Threshold hotspots ship `hidden`, validate the live state against direct-hash and synthetic-click abuse, share the scene's first-lock, keep 44px touch targets with mouse/Tab/Enter/Space, give verbatim feedback, and auto-advance to existing neighbouring scenes with no continue button; unlocks persist across reloads without re-announcing, and all three may be unlocked at once.

The annex debt settlement (v52) turns v51's three debts into reusable settlement capacity instead of a one-off anomaly unlock. Each kind's per-cycle capacity is `floor(v51 debt / 3)` (0–3, read-only — v52 never reduces or rewrites the v51 state), and once the total capacity reaches 3 a native **合签三债** button appears inside each annex room's brass tally plate (hidden, unfocusable and inert to synthetic clicks below the threshold; it never crowds, covers or hides the existing sixteen hotspots and shares the scene first-lock). Signing gives immediate verbatim feedback and walks straight into the **Annex Debt Clearinghouse** (`#annex-clearinghouse`) with no continue button. The clearinghouse's three pictured mechanisms are the native hotspots — the eye-shaped seal takes witness seals, the blank porcelain plate takes unnumbered seals, the reverse-stair model takes reverse seals — and each cycle settles exactly three deposits: buttons stay re-clickable until their kind's live capacity is used (then disabled with correct pressed/disabled aria), every legal deposit counts once, updates the three physical slot rows' accessible text in the picture and gives short feedback with no confirmation step. After the third deposit the feedback holds for one beat and the settlement auto-routes: a strict witness majority into the **Unreturned Witness Gallery**, unnumbered into the **Registry Before Zero**, reverse into the **Descending Appeals Stair**, and a rare exact 1/1/1 split onto the existing `#protocol-drift` (v42's direct hash is always allowed by design, so nothing is widened and v52 never reads or writes the v42 state). Each result room carries three more in-picture native-button actions over real objects (horn/seal/mirror-chair, blank plate/press/unnumbered door, reverse mirror/inverted gavel/silent bell) with verbatim feedback and ~0.7–1.0 s auto-advance (~0.3 s under reduced-motion), landing in the unseated listening booth, the witness carbon archive, the eyelid archive, the glyph niche (through one narrow v52 `lastScene`/`lastAction` exception in the v31 forecourt guard), the blank receipt press, the unnumbered floor, the reverse stairwell, the return audit and the bell-less ward. Reloads before the third deposit restore the exact allocation order and remaining capacity; a reload inside the settlement beat replays the verbatim feedback and re-arms the target exactly once; forged allocations normalize to a legal contiguous prefix (length ≤ 3, whitelist, per-kind live capacity) and `settled`/`outcome` are always re-derived, so forged saves can never enter a result room; direct-hash access to a result room requires the matching legal outcome or a past visit (otherwise it falls back to the clearinghouse, then to the eyelid archive when capacity is short), and direct-hash to the clearinghouse below the threshold falls back to the eyelid archive. Settled cycles restart from any annex room's 合签三债 with history kept. Everything lives in a fault-tolerant `goddead_v52_annex_settlement` state (cycle, allocations, settled, outcome, pending, four visited flags, witness/unnumbered/reverse/balanced history and nine action counters) that never reads or writes v28–v51 or main-line progress beyond the read-only v51 capacity; visited rooms restore the directory entries (`02φ / 三债结算`, `02χ / 无归见证`, `02ψ / 零前登记`, `02ω / 倒诉阶`), and Remembrance adds a single settlement memory line — still eight stat cards.

The route belief pollution (v53) deepens the v39 audit instead of adding another corridor: the rooms now believe you back. Every judgement the v39 audit truly accepts is also tallied by belief — following the rules (first/pale/blank) feeds 守则信任, following the senses (loud/pulse/named) feeds 感官诱信, and changing your answer on the same route feeds 自相矛盾 — in a fault-tolerant `goddead_v53_route_belief` state that only observes accepted v39 choices and never reads or writes v28–v52 or the main line; derived totals, variants and pending are always recomputed, so forged saves normalize themselves. The audit hub keeps its four v39 stats and gains three compact belief stats, and the three route cards plus the six judgement cards are now native-button hotspots pressed onto the real objects of the pictures (the horn corridor / turnstile / locker wall on the hub; the nearest door vs the loudest horn, the pale gap vs the red pulse, the blank plate vs the named plate on the routes) — same ids, same verbatim feedback, same v39 beats, no bottom cards left. Once a route reaches `>= 2` and a strict majority on one side, its figure atomically swaps to one of six mutated variants (1536×1024, ties fall back to the neutral v39 image) and a third threshold hotspot appears on the variant's central mechanism: the echo pedestal dial leads to the counter-knock gallery or its horn to the echo archive, the turnstile's pale gauge to the bell-less ward or its red gauge to the vein maintenance well, and the locker's blank plaque to the cloakroom of blank names or its weighing scale to the confession weighing room. Threshold clicks never count as v39 judgements — they only write v53 history, per-branch visit/action counts and a strict pending — and the three v29 branch scenes admit them through one narrow guard exception (current legal pending or a past visit); the other four targets needed no guard change, and `#protocol-drift` stays as open as v42 designed it. At 自相矛盾 >= 2 a **翻转核验牌** hotspot appears on the hub's brass route table and detours into `#protocol-drift` without touching the audit's progress. Everything ships `hidden` until live, keeps 44px targets with mouse/Tab/Enter/Space, verbatim feedback and the shared first-lock, and the directory gains `01τ½ / 归路信念` plus one Remembrance memory line — still eight stat cards.

The return pressure (v54) turns the three oldest branch rooms into one shared ledger instead of adding another corridor. The nine v29 choices move off their bottom cards and onto the pictures themselves — the echo archive's three brass listening wells, the well's handwheel and two levers, the weighing room's envelope/ash/empty pans — as native-button hotspots that keep every id, text, verbatim feedback, target and `lastChoice` semantic (plus a shared guard: only the first accepted on-scene click of a beat counts, so synthetic/off-scene/rapid calls are inert) while the old card containers are deleted outright. Every accepted choice is tallied in a fault-tolerant `goddead_v54_return_pressure` state (per-room scores, nine per-choice counters, breaches, a strict pending and a 16-entry history — only the five canonical parts are ever persisted; totals and approach are always re-derived, never trusted from storage) that never reads or writes v29/v30 or anything older. At approach ≥ 3 all three rooms atomically swap to their mutated approach bitmaps; at approach ≥ 6 — and only while the room holds a legal v29 `lastChoice` — a fourth hotspot appears on each mutation's new object (the wax-sealed dead receiver, the over-pressure gauge, the half-open drawer). Consuming it proves approach ≥ 6 before the click, banks exactly one breach (six points), and routes by the room's `lastChoice` into nine existing destinations (counter-knock gallery, lagging-shadow cloister, minute-before archive, seeping records, reverse laundry, bell-less ward, blank-receipt press, protocol drift, blank-name cloakroom — all guard-compatible with the current router, no guard touched); the pressure drops, the rooms calm down, and the cycle can be accumulated again. Each room carries a compact four-reading slip （回声累计 / 脉压 / 名重 / 迫近 x/6), the directory gains `01δ½ / 迫近回访`, and Remembrance adds one pressure line — still eight stat cards.

The floor reports back (v55) turns the three v35 duty rooms into an inspection loop instead of adding another corridor. All twelve existing buttons — three duty actions plus the v37 callback report in each of the bell-less ward, the seeping records and the reverse laundry — move onto the pictures themselves as native-button hotspots (the breathing bed / nightstand / IV line / bell-less device, the blank page / pool / drawer wall / sinking shaft, the drum / uniform / mirror cabin / right machine), keeping every id, text, feedback, target, signal/debt value and once-per-cycle semantics while the card containers are deleted. A new fault-tolerant `goddead_v55_floor_anomaly` state (canonical counters — verified, false reports, missed, streak, tendencies — are normalized and persisted; only contamination and the pending target are re-derived, never trusted from storage; a hardened strict pending cross-validated against settlement evidence and the canonical history) runs the inspection: once a room's duty is done for the cycle, re-entering puts it in inspection mode — a deterministic, reload-stable assignment (six legal patterns, always at least one anomalous and one normal room, rotated by cycle plus the follow/contain/submit tendencies you bank in the backrooms, never re-rolled and never drifting mid-cycle) decides whether the room shows its calm bitmap or one of three mutated anomaly bitmaps. Two in-picture report hotspots answer: report the candidate object (the call button on the sheet, the up-flowing black water, the white uniform in the right machine) or report the room normal — both can be genuinely wrong. Catching an anomaly pays +2 verification, a correct normal +1 (each with a v35 signal point); a false report costs a streak and a debt point, a missed anomaly two — with the v35 caps safely raised to 15/14 to hold the new theoretical maximums. Found or missed anomalies lead into three real backrooms — the underbed call station, the countersign drain and the negative laundry locker — each carrying three in-picture actions whose follow/contain/submit yields shape the next cycle's assignment, settling score only once per room per cycle while navigation stays open (the slip says 已结算). Direct hashes into the backrooms pass only a narrow guard (a current legal pending target or a past legal visit, else the unnumbered floor); reloads replay a legal beat exactly once; the directory gains `01ν½ / 床下回铃`, `01ξ½ / 反签排水`, `01ο½ / 负照更衣`, and Remembrance adds one inspection line — still eight stat cards.

The evidence audit (v56) deepens the inspection loop instead of adding rooms: each duty room in inspection mode now also offers a seal-funded evidence check pressed onto a real object (the call device, the nameless page's backlit frame, the right machine's service plate). A fault-tolerant `goddead_v56_evidence_audit` state (canonical fields only; credibility and the pending target re-derived; truth always read from the v55 assignment, never stored) freezes a per-cycle seal budget at the first inspection — 2 by default, 3 once your credibility (proofs + 2×blind + chains − contradictions − suppressed) reaches 4, just 1 at −2 — created once, reload-stable, never re-rolled. Spending a seal swaps the room in place to one of six deep bitmaps that simply show the truth, and reports are scored as proven or blind. When the third room's report settles with at least two deep checks done, the v55 landing is deliberately deferred and the beat detours into the **Symptom Handover Hub**, where three trays hand one symptom over: a sealed-and-correct handover or a blind-but-correct one opens the **Evidence Switchboard**, a false report drops into the false-positive shaft, and a missed anomaly is reclaimed by its backroom. The switchboard's press, furnace and human-shaped seal rack bank chains / suppressed / self-seals (self-sealing costs two exposure and returns you to the handover room's backroom), each settling score once per cycle while navigation stays open. Both new scenes sit behind the same narrow guard (a current legal pending target or a past legal visit, with the visit atomically recorded before pending is cleared), the slips show seals/evidence/blind/contradictions/exposure/credibility, the directory gains `01π½ / 症状交班` and `01ρ½ / 证据编组`, and Remembrance adds one audit line — still eight stat cards.

The consequence ledger (v57) grows bodies for the verdicts instead of letting them walk home. The v56 judgements keep every score semantic, but their destinations now detour through five new scenes behind a new fault-tolerant `goddead_v57_consequence_ledger` key (canonical fields only; liability and the pending target re-derived, and each ledger pending proven against the pre-click liability recorded in its canonical history entry — no fixed-delta back-computation, which the 9999 score cap would break). A false report or a burn now lands in the **Innocent Quarantine**, a missed handover in the **Omission Transfer Shaft**, and a press lands in the **Concordance Theatre** or the **Misbound Handover** depending on whether the handover was sealed-and-correct or merely blind (the kind is derived from canonical v56 state, never a new stored field); a self-seal always crosses the misbound table first. Each consequence scene offers three in-picture actions — twelve in all — that bank four responsibility scores (concordance / repair / transfer / self-burden) and walk onward to the vault, the shaft, protocol drift, the blank-name cloakroom, the blank-receipt press, the floor or the matching backroom, settling score only once per scene per cycle while navigation stays open (the slip says 已结清）. The first time a third distinct consequence scene settles, the ledger stores the original landing as a strict `deferredTarget` and the beat reroutes once into the **Liability Ledger**, where three more actions decide by pre-click liability: returning the verdict to its deferred landing (or to the vault/shaft by sign), signing it back onto yourself (≥4 to the vault, else back to the handover room's backroom), or assigning it to a vacancy (≤−2 to the shaft, else the cloakroom) — each with verbatim liability-quoting feedback. All five scenes sit behind the same narrow guard (a current legal pending target or a past legal visit, with visits atomically granted before pending is cleared), and all fifteen action listeners accept only trusted user clicks — a synthetic `HTMLElement.click()` is inert even on the active scene while mouse, touch, Tab+Enter and Space keep working — the directory gains five entries (`01σ½ / 共证剧场`, `01υ½ / 无辜留置`, `01φ½ / 漏报移交`, `01χ½ / 错绑交班`, `01ψ½ / 责任账房`), and Remembrance adds one ledger line — still eight stat cards.

The appeal registry (v58) lets the consequences be appealed instead of merely settled. Revisiting the **Liability Ledger** with a qualified book — at least three distinct consequence rooms settled and one ledger action banked, both re-derived from canonical v57 state, never stored — reveals an in-picture **objection seal** on the desk's red wax; breaking it records only a v58 entry beat (v57 scores untouched) and walks one beat later into the **Appeal Registry**, a non-linear hub behind a new fault-tolerant `goddead_v58_appeal` key (version/cycle/visits/history/pending only; the three appeal scores, settled rooms, close readiness, the v57 profile and the final target all re-derived from trusted current-cycle history and the live v57 state, pending targets and feedback recomputed verbatim and consumed once). The hub's identity device, twin dossiers and three-way dispatch desk lead freely into the **Identity Correction**, **Evidence Contradiction** and **Destination Review Shaft** rooms — three actions each, banking objection / precedent / contamination once per room per cycle, with revisits replaying feedback without scoring and an in-picture return hotspot walking back to the hub. History genuinely echoes: the hub brief and one feedback per room change with the v57-derived dominant responsibility (ties frozen as self-burden > transfer > repair > concordance), the current liability and the last consequence room. The chained ledger closes the case only after any two rooms settle — unlocked, never forced, never stealing the third room — and a pure routing function reads the pre-click scores: a perfect three-room 2/2/2 balance drops to the unnumbered floor, a strict high routes by liability sign or the last consequence room (theatre / misbound / drift / vault / false-positive / omission shaft), ties split across the ledger, the cloakroom and the misbound table, and empty states fall back to the ledger. All four scenes sit behind the same narrow guard (a current legal pending target or a past legal visit, visits atomically granted before pending clears), every listener accepts only trusted user clicks, the directory gains four entries (`01ω½ / 异议总署`, `01ωα / 身份勘误`, `01ωβ / 证据对照`, `01ωγ / 去向复核`), and Remembrance adds one appeal line — still eight stat cards.
The cross-examination (v59) lets settled review rooms be dug a second time — and lets the closing seal be stopped. Revisiting a settled v58 room reveals an in-picture **hearing seal** (hidden and inert before settlement or after the deep dig, synthetic clicks included); breaking it only swaps the scene's bitmap to the v59 deep image and reveals two deep hotspots over the real objects — the original three actions hide, the in-picture return hotspot stays, and nothing is scored or stored by the seal beat itself. Each room's two deep actions bank convergence / coercion / residue once per room per cycle with verbatim feedback and an auto-return to the registry; revisits keep the deep image, show the previous result, never double-score and never auto-advance. Once two or more rooms are deep, the appeal close is narrowly intercepted: the v58 target, feedback and close history are computed and appended exactly as before, only the pending transition is suppressed — the original verdict is stored as a strictly re-derived `deferredTarget` behind a new fault-tolerant `goddead_v59_cross_examination` key (version/cycle/visits/history/pending/deferredTarget only; every score, deep room, intercept and delivery state re-derived from trusted current-cycle history and the live v58 state, never persisted), and the visitor is sent one beat later to the **Cross-Examination Desk**. Its four in-picture actions route on the pre-click scores and only then bank +2: merging testimonies re-delivers the exact deferred verdict when convergence leads (otherwise the concordance theatre), marking coercion routes to protocol-drift or the misbound handover, archiving residue to the blank-name cloakroom or the evidence vault, and the **blank seat** — revealed only when all three rooms are deep and the three scores stand perfectly level (the unique reachable combination is frozen in the static suite) — sits down onto the unnumbered floor without scoring. Delivery happens exactly once: arrival clears the pending and the deferred target and backfills the destination's old guard credentials, and a later close is a brand-new verdict that no longer intercepts. All three listener groups (seals, deep actions, desk) accept only trusted user clicks, the desk direct hash sits behind a narrow guard that falls back to a legal appeal registry, pending entries are strict exact-key whitelists (seven keys for action and resolve, six for intercept) recomputed verbatim and consumed once (a delivery still in flight is not treated as delivered), forged or stale deferred targets drop themselves, v31–v58 state is never written (v58 only appends its own close history at the intercept point), the directory gains `01ωδ / 交叉听证`, and Remembrance adds one cross-examination line — still eight stat cards.
The chain of custody (v60) deepens the oldest result rooms instead of extending the chain. The **Evidence Preservation Vault** and the **False-Positive Shaft** — the last two scenes still stacking four button cards under a picture — are rebuilt as tactile stages: all eight existing buttons keep their ids, accessible texts, `aria-pressed` semantics, v33 marks/restart and v34 `startValuation` side effects, but now sit inside the picture on the real objects (the hanging evidence case, the return drawer, the broken chain-seal gap, the valuation tray; the retrieve drawer, the appendix scroll, the footprint plate, the asset tray), and the bottom card containers are deleted outright. A new fault-tolerant `goddead_v60_chain_of_custody` key (visited / scores / cycleActions<=8 / lastSource / lastAction / officeRuns / transfers / pending only) watches the eight source actions read-only: each action banks custody / revision / claim once per round, repeats keep their old destinations without re-scoring, and the first legal click in a round that reaches three distinct actions across both rooms — after the old side effects and with the old feedback verbatim plus one appended sentence — detains the destination into the new **Chain-of-Custody Office**. The office is an open scene holding three in-picture mechanisms (reseal press / amend drum / claim ring) that bank +2 and then route on the post-click scores: a strict high sends the chain to the retention vault, protocol drift or the returned-address cabinet, pairwise ties split across the false-confirmation desk, the witness-carbon archive and the blank-name cloakroom, and a perfect tie — including one completed by the action itself — returns the whole chain to the liability ledger with its old guard credential re-granted. A fourth **blank witness slot** appears only while the three scores stand level and positive (2/2/2 via seal + appendix + admission is the documented fresh-state path), scores nothing, and steps straight onto the unnumbered floor. Every handler validates the live scene, the pending and the first-lock before any side effect; the first accepted beat disables every hotspot in the scene; a strict four-field pending recomputes target and feedback verbatim, is consumed once across reloads and detours, and re-locks the scene's hotspots (restoring the chosen mechanism's pressed state) while it replays; leaving the office increments officeRuns and transfers, clears the round, and keeps the cumulative scores. The directory gains `01ωε / 证物链` on first office visit, Remembrance adds one custody line — still eight stat cards — and forget-all rebounds every v60 surface.
The failure reconstruction (v61) deepens the oldest and thinnest branch — the three v30 deep rooms — instead of adding floors. The **Distortion Transfer Chamber**, the **Reverse-Flow Pump Room** and the **Nameless Ledger Vault** keep all nine original actions byte-for-byte (ids, texts, feedback, targets, v29/v30 side effects, lastChoice, first-lock and guards), but their bottom card lists are deleted and the buttons now sit on the real objects of the original bitmaps. Each base image gains a visible reconstruction entry hotspot on a real object — the wax-seal zone, the red emergency-lamp zone, the foreground ledger drawers — and accepting it swaps the room to its v61 reconstruction bitmap: the three old actions and the entry hide, three evidence hotspots and an in-picture return hotspot appear. Evidence picks are atomic selections behind a new fault-tolerant `goddead_v61_failure_reconstruction` key (version/cycle/visited/selections/completedRuns/outcomeCounts/lastOutcome/replayRoom/pending only; the confirm / distort / blame scores, the desk sentence and the settle readiness are always derived, never stored), each room holds at most one selection, and every pick locks the room's hotspots, persists a strict per-kind pending and walks back one beat later to the new **Failure Reconstruction Desk**. The desk's three testimony boards are always clickable — relabeled 重选 once filled — and route straight back into the matching room's reconstruction state without ever dropping a stored pick; the master lever appears only when all three rooms hold legal selections, routes the derived totals through a frozen seven-way truth table (strict highs to the evidence vault, protocol drift or the liability ledger, pairwise ties to the false-confirmation desk, the witness-carbon archive or the blank-name cloakroom, a perfect 27-combo-enumerated tie to the chain-of-custody office), and only on arrival settles: completedRuns and outcomeCounts increment, the cycle turns over, selections clear, and the old guard credentials are re-granted through the narrowest existing helpers. All five new listener groups accept only trusted clicks, pending replays re-lock every hotspot and restore the chosen aria-pressed state, the desk's direct hash sits behind a narrow guard that cascades through the unmodified v30/v29 guards, the directory gains `02δ½ / 故障重演`, and Remembrance adds one reconstruction line — still eight stat cards.

The signal listens back (v62) deepens the three v46 side-tone rooms instead of adding floors. Once the **Unseated Listening Booth**, the **Unnumbered Jack Field** and the **Return-Ring Morgue** are all visited with at least three legal v46 marks, each base room reveals a fourth in-picture entry — the silent copper horns, the self-coiling wall lines, the breathing signal lamps — that shares the room's first-destination lock with the nine original actions (kept byte-for-byte) and auto-advances into the new **Listening-Back Console** (`#listening-back-console`). The console holds three circuit boards plus a foreground ground knife on one bitmap: picking a board swaps the matching room to its v62 listening-back bitmap (same 1536×1024, no layout shift), hides the old actions and the entry, and shows three response hotspots plus an in-picture 松线返回. Each room accepts exactly one response per cycle — no same-cycle replacement — behind a new fault-tolerant `goddead_v62_listening_back` key (version 62; cycle/visited/history/completedRuns/cutRuns/outcomeCounts/lastOutcome/replayRoom/pending only; the receive / feedback / silence / exposure scores, the board phrases and the lever readiness are always derived from the current cycle's trusted history, never stored). With exactly two rooms answered the ground knife appears as 提前接地 and spits the visitor back into the missing room (cutRuns and an early-* lastOutcome only); with all three it reads 让总台接听 and routes the derived scores through a frozen nine-way truth table — zero exposure to the blank-name cloakroom, five-plus to the witness-carbon archive, low-exposure strict highs to the counter-knock gallery or the blank-screen underarchive with ties back to the switchboard, high-exposure strict highs to proxy admission, protocol drift or the false-confirmation desk with ties to the chain-of-custody office — all 27 combinations enumerated and reachable (1/4/1/6/2/3/5/2/3). Settlement only lands on arrival: completedRuns and outcomeCounts increment, the cycle turns over, and ledger-scene targets regain their old guard credentials through the narrowest existing helper. All nineteen listener groups accept only trusted clicks, pending replays re-lock every hotspot and restore the chosen aria-pressed state, the console's direct hash sits behind a narrow guard that falls back through the unmodified v46 rooms, the directory gains `02μ½ / 反听总台`, and Remembrance adds one listening-back line — still eight stat cards.

A unified, scene-scoped auto-advance scheduler handles every transition: it gives ~0.9–1.4 s of perceptible feedback in normal mode, shortens but remains understandable under reduced-motion, clears its timer on scene exit or repeated triggers, and never jumps on its own when persistent state is restored or a scene is opened directly by hash. Session-scoped consumed markers are only flipped inside each timer's `before` callback, so a transition cancelled by back-navigation can be re-armed by repeating the core action; persistent-state restoration or direct hash entry never arms a transition. Each switch scrolls to the top of the new scene and moves focus to its title. Only the directory drawer and explicit back-navigation remain; cross-scene shortcut buttons and bottom forward buttons on the linear path have been removed.

After the main line is complete, Remembrance offers the **Governance Protocol** (v28, 神圣平衡与代理神明协议) to returning visitors. Three acting-deity rulings — at the Acting Deity Desk, the Offering furnace, and the Sacred Reliquary Vault — shift three derived resources (灵质 E / 灰烬 A / 共鸣 R) tracked on a governance HUD. Each ruling auto-advances after a short feedback beat; the continue buttons are optional fallbacks, never a required second step. The final balance resolves into one of four collectible endings (Ascension / Madness / Oblivion / Nightwatch) or a Divine Collapse. Endings persist into a collection; starting a new cycle or retrying a collapse resets only the current cycle's rulings and increments `cycleCount` exactly once, preserving the collection and all main-line progress (watch, line four, dead-letter, cancellation, appointment, prayers, relics, arrivals).

For short desktop viewports (≤800px height), the later scenes switch to a compact two-column layout so the first actionable control is visible without scrolling, while taller/wider screens keep the cinematic vertical staging. Stat-card text values stay inside their cells, and scene-title focus rings use the site's blood/bone palette instead of the browser default blue.

## Deploy

This repository is intended to deploy through Cloudflare Pages.

- Build command: none
- Output directory: `/`
- Custom domain: `goddead.com`
- Pages URL: [goddead.pages.dev](https://goddead.pages.dev/)
- WWW URL: [www.goddead.com](https://www.goddead.com/)
- DNS provider: Cloudflare
- Nameservers: `sam.ns.cloudflare.com`, `suzanne.ns.cloudflare.com`
- Current domain status: [goddead.com](https://goddead.com/) and [www.goddead.com](https://www.goddead.com/) are live on Cloudflare Pages.

## Local Check

```bash
node --check script.js
node tests/site.test.mjs
git diff --check
```

Visual QA evidence and the final comparison report live in `design-qa-evidence/` and `design-qa.md`.

## v63 — 终局退件所 / THE ENDING RETURNS UNOPENED

After the visitor has reached at least one of the four v28 endings (`ascension`, `madness`, `oblivion`, `nightwatch`), the **Remembrance** room reveals a new directory entry — the **Ending Return Office** (`#ending-return-office`). The office is a small filing counter where each reached ending can be handled in three ways: accepted as received, returned to the moment it began, or misrouted into silence. Each of the four endings × three handlers produces one of twelve **coda** entries in canonical `baseEnding:action` form (`ascension:accept`, `ascension:return`, `ascension:misroute`, `madness:accept`, `madness:return`, `madness:misroute`, `oblivion:accept`, `oblivion:return`, `oblivion:misroute`, `nightwatch:accept`, `nightwatch:return`, `nightwatch:misroute`). Collecting at least one coda from all four base endings unlocks a hidden thirteenth ending, **无终局 · UNENDING**, behind the **Unending Gallery** (`#unending-gallery`). The gallery offers three exits; the reverse stair falls back to the v62 listening-back console only when the v62 console guard is satisfied, otherwise to the echo switchboard. All v63 runtime state lives in the single fault-tolerant key `goddead_v63_ending_return` (version 63) with a canonical ten-field projection. v63 only reads the current v28 `resultStatus` through the existing validator and reads the v62 listening-back guard when routing the reverse stair; it never writes the v28 governance key or any earlier state key. The two new scenes bring the total from 87 to 89. Assets are `assets/v63-ending-return-office.webp` and `assets/v63-unending-gallery.webp` (both 1536×1024, sha256 frozen in `tests/site.test.mjs`).

## v64 — 因果倒邮 / THE ENDING ARRIVED EARLY

Once the thirteenth v63 ending **无终局 · UNENDING** is unlocked, the **Remembrance** room gains a single new button — **把无终局寄回最初** — leading into the **Causal Sorter** (`#causal-sorter`). The visitor chooses one of three mailing modes (accept, return, misroute) and then one of four early destinations (threshold, protocol, watch, offering), producing 3 × 4 = 12 **early-arrival consequences** such as *先到的回执 · RECEIPT BEFORE KNOCK* and *错投神前 · PRAYER MISROUTED BEFORE GOD*. After all four destinations have received at least one consequence, the **First Draft Vault** (`#first-draft-vault`) opens a **Version Zero** slot; the **Before the First Knock** (`#before-first-knock`) scene then offers three zero-ending actions (*answer*, *file-death*, *take-seat*), for a total of 15 new codex entries. Every dispatch leaves an optional **future echo stamp** on the target old scene that can return the visitor to the sorter. All v64 runtime state is isolated in the single fault-tolerant key `goddead_v64_causal_mail` (version 64) with a canonical eleven-field projection; it only reads the v63 `unendingUnlocked`/`codas` evidence and never writes the v63 key or any earlier state key. The three new scenes bring the total from 89 to 92. Assets are `assets/v64-causal-sorter.webp`, `assets/v64-first-draft-vault.webp`, and `assets/v64-before-first-knock.webp` (all 1536×1024, sha256 frozen in `tests/site.test.mjs`). Browser QA is partially verified: guard rewrite, layout/asset dimensions, and activeEcho text rendering pass with zero console warnings/errors, but protected real-click transitions remain pending manual verification because all 16 v64 click listeners require trusted user events.

## v65 — 因果疤痕 / THE CAUSE LEFT A SCAR

After a v64 destination has collected all three of its early-arrival modes (`accept:destination`, `return:destination`, `misroute:destination`), the corresponding old scene grows an independent **causal scar stage**: the threshold, protocol, watch, and offering rooms each reveal three native hotspots — **stitch** the cause, **drain** the consequence, or **graft** a witness — producing 4 × 3 = 12 **scar treatments** such as *缝回第一敲 · FIRST KNOCK SUTURED* and *炉口作证 · THE FURNACE TESTIFIED*. Once all four old scenes have at least one treatment, **Remembrance** reveals **让四道疤互相指认**, leading into the new **Causeless Ward** (`#causeless-ward`) and its three room endings (*become-cause*, *refuse-cause*, *ending-adopts*), for a total of 15 new codex entries. All v65 runtime state lives in the single fault-tolerant key `goddead_v65_causal_scars` (version 65) with a canonical eight-field projection; `scarEligible` is derived only from the v64 `outcomes` whitelist and v65 never writes the v64 key or any earlier state key. The single new scene brings the total from 92 to 93. Assets are `assets/v65-threshold-causal-scar.webp`, `assets/v65-protocol-causal-scar.webp`, `assets/v65-watch-causal-scar.webp`, `assets/v65-offering-causal-scar.webp`, and `assets/v65-causeless-ward.webp` (runtime WebP 1536×1024 produced from source PNGs; threshold/watch sources were 1528×1029 / 1537×1023 and normalized with LANCZOS before Pillow q85 method=6 encoding; the other three sources are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Browser QA by Codex's built-in browser on the local production build (`http://127.0.0.1:4173`) is partially verified: desktop 1280×720 and mobile 390×844 show all five scenes active, five WebP assets complete at 1536×1024, all hotspots inside their figures, non-overlapping, and ≥44px, with `scrollWidth` equal to the viewport; Remembrance displays the entry enabled, 4/15 codex cells unlocked, and the exact memory line; direct `#causeless-ward` is guarded back to `#remembrance`; malformed `treatments`/`roomOutcomes` types render without error; console warning/error is 0. Pending-replay paths (entry source remembrance, treatment source threshold, ending source ward, ending target threshold) restore feedback, lock the correct buttons, advance once, and update counts to 5/12 and remembrance 1/3. However, automated Playwright/CUA clicks are rejected by the 16 `e.isTrusted` guards (verified zero side effects), so clean-state real user clicks, double-click races, and keyboard Enter/Space remain pending manual verification; the replay checks were performed by loading saved states, not by clean clicks.

## v66 — 反事实纺生 / THE LIFE THAT NEVER HAPPENED

Once all 12 v65 scar treatments and all 3 Causeless Ward endings are collected, the scars stop waiting and start spinning out lives that never happened. The visitor picks an origin (threshold, protocol, watch, or offering), a method (stitch, drain, or graft), and an inheritance (cause, consequence, or ending), producing 4 × 3 × 3 = 36 **counterfactual lives** such as *门前未生 · UNBORN AT THE THRESHOLD · 缝骨 · STITCH-BONED · 成因生 · CAUSE-BORN*. Each completed life leaves an **unlived echo** in its origin scene, echoing the full narrative and offering a return to the spindle. After covering all four origins, three methods, and three inheritances (possible in as few as four spins), **Remembrance** reveals **让所有未活之生互认亲属**, opening the **Life Without Cause** room (`#life-without-cause`) and its three meta-endings: *群生着身 · THE MANY LIVES WEAR YOU*, *未生自葬 · THE UNLIVED BURY THEMSELVES*, and *纺锤不眠 · THE SPINDLE OUTLIVES ENDINGS*. v66 adds four new scenes (`#counterfactual-spindle`, `#scar-loom`, `#unlived-nursery`, `#life-without-cause`), bringing the total from 93 to 97, and 39 new codex cells (36 lives + 3 meta-endings) after the v15 scar codex. All v66 runtime state lives in the single fault-tolerant key `goddead_v66_counterfactual_lives` (version 66) with a canonical ten-field projection; `counterfactualLivesUnlocked` reads only the v65 state and v66 never writes the v65 key or any earlier state key. Assets are `assets/v66-counterfactual-spindle.webp`, `assets/v66-scar-loom.webp`, `assets/v66-unlived-nursery.webp`, and `assets/v66-life-without-cause.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Browser QA by Codex's built-in browser on the local production build (`http://127.0.0.1:4173`) is verified: desktop 1280×720 and mobile 390×844 show all four scenes routed correctly, four WebP assets complete at 1536×1024, all hotspots inside their figures, non-overlapping, and ≥44px, with no horizontal overflow; Remembrance displays the 39-cell codex, the memory line, both entry buttons, and the four directory links correctly; direct hashes without v65 evidence are guarded back to `#remembrance`; all seven pending kinds replay and settle correctly without duplicate counts; console warning/error is 0. Programmatic Playwright clicks are correctly ignored by the 19 `e.isTrusted` guards with zero side effects; real transitions are covered by pending source/target browser replay.

## v67 — 无血家谱 / THE FAMILY THAT NEVER SHARED BLOOD

After all three v66 meta-endings are collected, the unlived lives stop being solitary and start choosing kin. The visitor first picks a root (spindle, loom, nursery, or room), then a bond (ancestor, twin, or descendant), and finally a memory inheritance (keep, exchange, or return), producing 4 × 3 × 3 = 36 **bloodless records** such as *纺锤为根 · ROOTED IN THE SPINDLE · 晚祖 · ANCESTOR-CLAIMED · 留年 · CHILDHOOD-KEPT*. Each completed record leaves a **bloodless kin echo** in the corresponding v66 scene, echoing the full narrative and offering a return to the genealogy hall. After covering all four roots, three bonds, and three memories (possible in as few as four records), **Remembrance** reveals **让三十六页家谱互认血缘**, opening the **Last Family Court** (`#last-family-court`) and its three family endings: *众生认祖 · ALL LIVES DESCEND FROM YOU*, *后代继承你 · THE DESCENDANTS INHERITED YOU*, and *万世成孤 · EVERY ERA BECAME AN ORPHAN*. v67 adds four new scenes (`#counterfactual-genealogy`, `#bloodless-archive`, `#borrowed-childhood`, `#last-family-court`), bringing the total from 97 to 101, and 39 new codex cells (36 records + 3 family outcomes) after the v66 codex. All v67 runtime state lives in the single fault-tolerant key `goddead_v67_bloodless_genealogy` (version 67) with a canonical ten-field projection; `bloodlessGenealogyUnlocked` reads only the v66 `getCounterfactual()` state and v67 never writes the v66 key or any earlier state key. Assets are `assets/v67-counterfactual-genealogy.webp`, `assets/v67-bloodless-archive.webp`, `assets/v67-borrowed-childhood.webp`, and `assets/v67-last-family-court.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Static tests run 5744 assertions, covering asset hashes/sizes, 101 scenes, 36+3 frozen entries, the ten-key state, strict unlock from v66 coverage plus three meta-outcomes, seven pending kinds, source/target/else replay, activeKin reconstruction, 19 `e.isTrusted` guards, narrow-screen hotspot dimensions, a four-root kin-return DOM replay regression, and a Remembrance first-round regression that verifies the codex box/entry parent and regular entry button are visible/enabled at zero own-progress while the v66 unlock is satisfied. A real browser audit caught that both v66 and v67 hid the codex parent at zero progress, making the entry button layout 0×0; the fix makes `paintCounterfactualCodex`/`paintBloodlessCodex` visible whenever the version's unlock is true. Browser QA by Codex's built-in browser on the local production build (`http://127.0.0.1:4173`) is now fully verified: desktop 1280×720 and mobile 390×844 show all four scenes routed correctly, four WebP assets complete at 1536×1024 with no crop/overflow, all hotspots inside their figures, non-overlapping, and ≥44px on mobile, with `scrollWidth` equal to the viewport and zero console warning/error. Remembrance confirms the v67 zero-progress entry is truly visible and clickable, coverage progresses to 4/36, the 39-cell codex renders, directory links show/hide correctly, family endings reach 1/3 and remain idempotent on reload; the same zero-progress Remembrance entry regression was re-verified for v66. Locked direct hashes and malformed saves normalize safely; all seven pending kinds replay and settle correctly; the four-root kin-return DOM mapping fix is confirmed in the real browser; programmatic Playwright clicks are still rejected by the 19 `e.isTrusted` guards with zero side effects.

## v68 — 世代借贷 / THE GENERATIONS BORROW AGAINST DEATH

After all three v67 family endings are collected, the bloodless family stops asking who gave birth to whom and starts asking who can still pay for whom. The visitor first picks an era (past, present, or future), then a collateral (seven years, a death date, or an unheld funeral), and finally a repayment term (childhood interest, birth payment, or descendant rollover), producing 3 × 3 × 3 = 27 **generation loans** such as *逝代 · 七年 · 童年付息*. Each completed loan leaves an independent **collection notice** in the corresponding old scene (threshold, remembrance, or unending-gallery), echoing the full narrative and offering a return to the credit office. After covering all three eras, three collaterals, and three terms (possible in as few as three diagonal loans), **Remembrance** reveals **让所有未还年岁同时到期**, opening the **Age Foreclosure Court** (`#age-foreclosure-court`) and its three foreclosure outcomes: *收走当下 · REPOSSESS THE PRESENT*, *让死亡破产 · BANKRUPT DEATH*, and *让葬礼继承家族 · LET THE FUNERAL INHERIT THE FAMILY*. v68 adds four new scenes (`#generational-credit-office`, `#lifetime-pawn-vault`, `#mortality-clearing-house`, `#age-foreclosure-court`), bringing the total from 101 to 105, and 30 new codex cells (27 loans + 3 foreclosure outcomes) after the v67 codex. All v68 runtime state lives in the single fault-tolerant key `goddead_v68_generation_loans` (version 68) with a canonical eleven-field projection; `generationLoansUnlocked` reads only the v67 `getBloodless()` state and v68 never writes the v67 key or any earlier state key. Assets are `assets/v68-generational-credit-office.webp`, `assets/v68-lifetime-pawn-vault.webp`, `assets/v68-mortality-clearing-house.webp`, and `assets/v68-age-foreclosure-court.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Static tests run 5986 assertions, covering asset hashes/sizes, 105 scenes, 27+3 frozen entries, the eleven-key state, strict unlock from v67 coverage plus the three required family outcomes, seven pending kinds, source/target/else replay, activeNotice re-computation, 17 `e.isTrusted` guards, synthetic click zero side effects, Remembrance codex/dual entries/directory, forget-all cleanup, and narrow-screen ≥44px hotspots. Independent browser evidence QA was completed on the local production build, while clean positive real-user clicking remains pending: desktop 1280×720 and real 390×844 iframe viewport show all four scenes active with 1536×1024 images complete, hotspots in-frame/non-overlapping/≥44px, zero horizontal overflow; locked state hides v68 UI/codex/entry/notices; ready-zero shows the entry visible and enabled with all 30 codex cells locked and the foreclosure entry hidden; ready-coverage reveals the foreclosure entry, stats, and 30-cell codex; balances confirmed 1/27/loanRuns 1/years 7 for the first loan and 1/27/loanRuns 2/years 14 for a repeated loan; notice-return cleared the threshold notice; a representative foreclosure (seize-present) passed 1/3; bad JSON reset safely; seven pending kinds behaved correctly on source/target/else with source single-scheduling covered by Node isolation tests; synthetic clicks were rejected by `e.isTrusted` with zero side effects; console warning/error remained zero.

## v69 — 死后人口普查 / THE DEAD COUNT WHETHER YOU WERE BORN

After all three v68 foreclosure outcomes are collected, the dead, the living, and the unborn are summoned to a posthumous census that refuses live testimony. The visitor first picks an electorate (departed, living, or unborn), then a piece of contradictory evidence (birth certificate, other people's memories, or a blank register), and finally a verdict (count as born, count as never born, or assign the birth elsewhere), producing 3 × 3 × 3 = 27 **posthumous census records** such as *逝代 · 出生证 · 已出生*. Each completed ballot leaves an independent **census summons** in the corresponding old v68 scene (lifetime-pawn-vault, generational-credit-office, or mortality-clearing-house), echoing the full narrative and offering a return to the census hall. After covering all three electorates, three evidences, and three verdicts (possible in as few as three diagonal records), **Remembrance** reveals **让三册人口互相注销**, opening the **Population Nullification Court** (`#population-nullification-court`) and its three nullification outcomes: *追认这次出生 · RATIFY THIS BIRTH*, *把来访者移出人口 · REMOVE THE VISITOR FROM THE POPULATION*, and *让空白户籍成为公民 · ENFRANCHISE THE BLANK REGISTER*. v69 adds four new scenes (`#posthumous-census-hall`, `#contradictory-evidence-archive`, `#birth-ballot-booth`, `#population-nullification-court`), bringing the total from 105 to 109, and 30 new codex cells (27 records + 3 nullification outcomes) after the v68 codex. All v69 runtime state lives in the single fault-tolerant key `goddead_v69_posthumous_census` (version 69) with a canonical eleven-field projection; `posthumousCensusUnlocked` reads only the v68 `getGenerationLoans()` state and v69 never writes the v68 key or any earlier state key. Assets are `assets/v69-posthumous-census-hall.webp`, `assets/v69-contradictory-evidence-archive.webp`, `assets/v69-birth-ballot-booth.webp`, and `assets/v69-population-nullification-court.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`).

## v70 — 亡者议会 / PARLIAMENT OF THE DEAD

After the v69 posthumous census has covered all three electorates, three evidences, and three verdicts and collected the three nullification outcomes, the dead population is divided into three ghost caucuses: the late-born, the removed visitors, and the blank-citizens. The visitor first picks a caucus, then one of four citizenship motions (right to a name, right to cast a shadow, right to possess a body, right to die only once), and finally a citizen register target (name, shadow, or body), producing 3 × 4 × 3 = 36 **dead parliament decrees** such as *迟生者选区 / 姓名权 / 归姓名*. Each completed decree leaves an independent **parliament whip** in the corresponding old v69 scene (birth-ballot-booth, posthumous-census-hall, or contradictory-evidence-archive), echoing the full narrative and offering a return to the rotunda. After covering all three caucuses, four motions, and three citizen registers (possible in as few as four representative decrees), **Remembrance** reveals **让三种公民权互相否决**, opening the **Three-Person Republic Court** (`#three-person-republic-court`) and its three constitutional crisis outcomes: *让姓名成为唯一公民 · LET THE NAME BE THE ONLY CITIZEN*, *让影子另组反对共和国 · LET THE SHADOW FOUND AN OPPOSITION REPUBLIC*, and *让肉身废除死者选举 · LET THE BODY ABOLISH DEAD SUFFRAGE*. v70 adds four new scenes (`#dead-parliament-rotunda`, `#citizenship-article-chamber`, `#constitutional-severance-desk`, `#three-person-republic-court`), bringing the total from 109 to 113, and 39 new codex cells (36 decrees + 3 crisis outcomes) after the v69 codex. All v70 runtime state lives in the single fault-tolerant key `goddead_v70_dead_parliament` (version 70) with a canonical eleven-field projection; `deadParliamentUnlocked` reads only the v69 `getPosthumousCensus()` state and v70 never writes the v69 key or any earlier state key. Assets are `assets/v70-dead-parliament-rotunda.webp`, `assets/v70-citizenship-article-chamber.webp`, `assets/v70-constitutional-severance-desk.webp`, and `assets/v70-three-person-republic-court.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Static tests run 6502 assertions, covering asset hashes/sizes, 113 scenes, 36+3 frozen entries, the eleven-key state, seven classes of strict pending, activeWhip re-projection, eighteen isTrusted-guarded click listeners, the three v69 old-scene guard bridges, Remembrance codex/dual entry/directory/forget-all behavior, and mobile hotspot sizing. Codex independently verified the v70 real-browser acceptance suite on the local production files: locked/ready-zero/four scenes at 1280×720 and 390×844/isTrusted real-mouse entry/representative decree/repeat decree/three caucus whips/coverage/representative crisis/seven pending kinds/bad JSON/clean console all passed, while synthetic clicks were rejected.

## v71 — 死亡外交部 / MINISTRY OF MORTAL DIPLOMACY

After the v70 dead parliament has covered all three caucuses, four motions, and three citizen registers and collected the three constitutional crisis outcomes, the name republic, the shadow opposition republic, and the body regime each send an incomplete legation to three impossible states: the country before birth, the republic after memory, and the state inside the body. The visitor first picks a delegation (the bodiless name legation, the exiled shadow mission, or the nameless body consulate), then one of three counterparts (before-birth country, after-memory republic, or inside-body state), and finally one of four treaty clauses (mutual recognition, asylum for the unlived, resurrection embargo, or extradite the ending), producing 3 × 3 × 4 = 36 **death diplomacy treaties** such as *无身姓名使团 / 出生以前的国 / 互相承认*. Each completed treaty leaves an independent **diplomatic courier** in the corresponding old scene (counterfactual-spindle, remembrance, or constitutional-severance-desk), echoing the full narrative and offering a return to the ministry. After covering all three delegations, three counterparts, and four clauses (possible in as few as four representative treaties), **Remembrance** reveals **召回所有从未被承认的使节**, opening the **Undeclared War Room** (`#undeclared-war-room`) and its three war outcomes: *把所有边境移进肉身 · MOVE EVERY BORDER INSIDE THE BODY*, *只承认流亡者 · RECOGNIZE ONLY THE EXILE*, and *把复活列为跨境罪 · CRIMINALIZE RESURRECTION*. v71 adds four new scenes (`#death-foreign-ministry`, `#nonexistent-border-chancery`, `#treaty-autopsy-table`, `#undeclared-war-room`), bringing the total from 113 to 117, and 39 new codex cells (36 treaties + 3 war outcomes) after the v70 codex. All v71 runtime state lives in the single fault-tolerant key `goddead_v71_death_diplomacy` (version 71) with a canonical eleven-field projection; `deathDiplomacyUnlocked` reads only the v70 `getDeadParliament()` state and v71 never writes the v70 key or any earlier state key. Assets are `assets/v71-death-foreign-ministry.webp`, `assets/v71-nonexistent-border-chancery.webp`, `assets/v71-treaty-autopsy-table.webp`, and `assets/v71-undeclared-war-room.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Static tests run 6881 assertions, covering asset hashes/sizes, 117 scenes, 36+3 frozen entries, the eleven-key state, seven classes of strict pending, activeCourier re-projection, eighteen isTrusted-guarded click listeners, the three old-scene guard bridges, Remembrance codex/dual entry/directory/forget-all behavior, and mobile hotspot sizing.

**v71 状态：** 已实现，Codex 独立浏览器验收通过（6881 assertions）。四新场景桌面 1280×720 与移动 390×844 图片、热区、无横溢均通过，热区最小 44px；四 direct hash 锁定回 remembrance；代表条约签署/目标结算/重复/刷新幂等、三处信使直达与返程、activeCourier 双入口 visible+disabled、战争门槛/未解锁/平局/战争结局幂等、坏 JSON/错误类型回默认、错误场景 pending 清理、合成点击零副作用、Chrome 真实鼠标入口成功、新鲜页 warn/error=0 均通过。

## v72 — 遗言中央银行 / CENTRAL BANK OF LAST WORDS

After the v71 death diplomacy has covered all three delegations, three counterparts, and four treaty clauses and collected the three undeclared-war outcomes, the name republic, the shadow opposition republic, and the body regime discover that no existing currency can pay for cross-border death. The visitor first picks a last-word currency (ownerless signature note, unseen shadow coin, or unspoken testament bond), then one of three impossible reserves (last breath, inherited silence, or collateralized ending), and finally one of four monetary policies (issue before speaking, devalue the farewell, freeze resurrection liquidity, or redeem in another mouth), producing 3 × 3 × 4 = 36 **last-word financial instruments** such as *无主签名钞 / 最后一口气 / 先于说出口发行*. Each completed instrument leaves an independent **remittance teller** in the corresponding old scene (threshold, remembrance, or unending-gallery), echoing the full narrative and offering a return to the central bank. After covering all three currencies, three reserves, and four policies (possible in as few as four representative instruments), **Remembrance** reveals **让所有遗言同时违约**, opening the **Sovereign Default Chamber** (`#sovereign-default-chamber`) and its three macro outcomes: *国有化所有遗言 · NATIONALIZE EVERY LAST WORD*, *让沉默决定利率 · LET SILENCE SET THE INTEREST RATE*, and *宣布死亡大到不能倒闭 · DECLARE DEATH TOO BIG TO FAIL*. v72 adds four new scenes (`#last-word-central-bank`, `#unsaid-currency-mint`, `#testament-clearing-vault`, `#sovereign-default-chamber`), bringing the total from 117 to 121, and 39 new codex cells (36 instruments + 3 default outcomes) after the v71 codex. All v72 runtime state lives in the single fault-tolerant key `goddead_v72_last_word_bank` (version 72) with a canonical eleven-field projection; `lastWordBankUnlocked` reads only the v71 `getDeathDiplomacy()` state and v72 never writes the v71 key or any earlier state key. Assets are `assets/v72-last-word-central-bank.webp`, `assets/v72-unsaid-currency-mint.webp`, `assets/v72-testament-clearing-vault.webp`, and `assets/v72-sovereign-default-chamber.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are listed with sha256 budget in `docs/V72LastWordCentralBankDesign.md`).

**v72 状态：** 已实现，Codex 独立浏览器验收通过（7357 assertions）。四新场景桌面 1280×720 与移动 390×844 图片、热区、无横溢均通过，热区最小 44px；四 direct hash 锁定回 remembrance；ready-zero 39 格全锁、普通入口可用；真实 isTrusted 鼠标入口；entry/currency/reserve/instrument 来源刷新重播、threshold 目标刷新与返回；同一 instrument 重复仍图鉴 1、monetaryRuns 真实增长；3 份 coverage 失败、4 份成功、多数/并列；default-entry 与 default 的来源刷新、目标刷新、重复结局去重；threshold/remembrance/unending-gallery 三汇兑员直达/刷新/返回；remembrance 无 coverage activeRemittance 时普通+违约入口 visible+disabled、返回 enabled、清 active 后按 coverage 恢复；draft 中途两入口 disabled、违约庭 figure/tally visible 但 action disabled；坏 JSON/数组回默认且 deep hash 回 remembrance；pending else 清理；图鉴 policy title 修复后四标题无 undefined；应用自身 console warn/error=0（沉浸式翻译扩展 dynamic-i18n version mismatch 为第三方扩展噪声，不计入）。四新场景已接入预加载/目录/痕迹/图鉴；四源图/WebP 哈希与预算已锁定；121 场景、36+3 冻结文案、十一键状态、七类 strict pending、activeRemittance 必须已收集三键反算、18 组 isTrusted 防线、合成 click 零副作用、default outcome settle/repeat、bank/mint/vault/default UI disabled 与 handler 一致（activeRemittance/draft/pending 不生成假按钮；draft 非空时 entry/default-entry/default 均被拒绝/禁用）、forget-all 清 v72、窄屏热点 ≥44px 与三处旧场景汇兑员回流均已覆盖。

## v73 — 梦境海关总署 / CUSTOMS OF BORROWED DREAMS

After the v72 central bank has covered all three currencies, three reserves, and four policies and collected the three sovereign-default outcomes, the dead can finally pay cross-border fees, but they immediately begin buying dreams that do not belong to them. The visitor first picks a dream passport (dead-god dream passport, unborn child sleep visa, or future-witness night pass), then one of three contraband dream-objects (face never seen awake, memory that kept dreaming, or ending without a dreamer), and finally one of four nightmare tariffs (tax years awake, confiscate the dreamer, re-export the dream to death, or grant nightmare asylum), producing 3 × 3 × 4 = 36 **dream declarations** such as *神死前旧梦 / 醒时未见之脸 / 以清醒年岁征税*. Each completed declaration leaves an independent **customs inspector** in the corresponding old scene (eyelid-archive, remembrance, or unending-gallery), echoing the full narrative and offering a return to the customs house. After covering all three passports, three contrabands, and four tariffs (possible in as few as four representative declarations), **Remembrance** reveals **把所有清醒者驱逐出梦境**, opening the **Waking Deportation Yard** (`#waking-deportation-yard`) and its three final verdicts: *让所有噩梦成为公民 · NATURALIZE EVERY NIGHTMARE*, *把做梦者驱逐出梦 · DEPORT THE DREAMER FROM THE DREAM*, and *把清醒列为违禁品 · CRIMINALIZE WAKING*. v73 adds four new scenes (`#borrowed-dream-customs`, `#contraband-sleep-terminal`, `#nightmare-tariff-bureau`, `#waking-deportation-yard`), bringing the total from 121 to 125, and 39 new codex cells (36 declarations + 3 deportation outcomes) after the v72 codex. All v73 runtime state lives in the single fault-tolerant key `goddead_v73_dream_customs` (version 73) with a canonical eleven-field projection; `borrowedDreamCustomsUnlocked` reads only the v72 `getLastWordBank()` state and v73 never writes the v72 key or any earlier state key. Assets are `assets/v73-borrowed-dream-customs.webp`, `assets/v73-contraband-sleep-terminal.webp`, `assets/v73-nightmare-tariff-bureau.webp`, and `assets/v73-waking-deportation-yard.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`). Static tests run 7880 assertions, covering asset hashes/sizes, 125 scenes, 36+3 frozen entries, the eleven-key state, strict unlock from v72 coverage plus three default outcomes, seven pending kinds, source/target/else replay, activeInspector collected membership, 18 isTrusted guards, synthetic-click no-op, deportation outcome settle/repeat, customs/terminal/bureau/yard UI disabled matching handlers, Remembrance dual-entry/catalog/memory/codex, forget-all cleanup, and narrow-screen hotspots ≥44px.

**v73 状态：** 已实现，Codex 独立浏览器验收通过（7880 assertions）。Chrome 真实点击走通入口 → 护照 → 违禁梦 → 关税 → 旧场景 → 梦检员返回；锁态、ready-zero、3/4 与 4/4 覆盖门槛、三个梦检员、三个驱逐目标、pending 重播、刷新不重复、坏 JSON/数组回落均通过；1280×720 与 390×844 四幕布局通过，图片完整、无横向溢出、热点不重叠且 ≥44px；页面自身无 console error/warning；Chrome 仅出现 Immersive Translate 扩展自己的版本报错，与项目无关。

## v74 — 墓碑专利局 / PATENT OFFICE OF THE UNBURIED

After the v73 customs house has declared waking contraband and collected the three waking-deportation outcomes, the seized dreams are reclassified as prior art for inventions that were never made. The visitor first picks an impossible applicant (unborn inventor, posthumous inventor, or future plagiarist), then one of three non-existent prior-art artifacts (uncarved epitaph blueprint, dream-worn prototype, or descendant memory machine), and finally one of four impossible claims (own the unmade, license death as a user, sue the future for copying backward, or forbid the inventor to invent), producing 3 × 3 × 4 = 36 **impossible patents** such as *未生发明人 / 未刻墓志图纸 / 主张拥有未造之物*. Each completed patent leaves an independent **tombstone examiner** in the corresponding old scene (threshold, eyelid-archive, or remembrance), echoing the full narrative and offering a return to the patent office. After covering all three applicants, three prior-art artifacts, and four claims (possible in as few as four representative patents), **Remembrance** reveals **把存在本身送进永久许可终审**, opening the **Perpetual License Tribunal** (`#perpetual-license-tribunal`) and its three perpetual-license rulings: *让发明拥有自己 · GRANT THE INVENTION OWNERSHIP OF ITSELF*, *把既往存在全部无效 · INVALIDATE ALL PRIOR EXISTENCE*, and *许可未葬者附身原型 · LICENSE THE UNBURIED TO HAUNT EVERY PROTOTYPE*. v74 adds four new scenes (`#tombstone-patent-office`, `#prior-art-ossuary`, `#impossible-claim-examination`, `#perpetual-license-tribunal`), bringing the total from 125 to 129, and 39 new codex cells (36 patents + 3 ruling outcomes) after the v73 codex. All v74 runtime state lives in the single fault-tolerant key `goddead_v74_tombstone_patent_office` (version 74) with a canonical eleven-field projection; `tombstonePatentOfficeUnlocked` reads only the v73 `getDreamCustoms()` state and v74 never writes the v73 key or any earlier state key. Assets are `assets/v74-tombstone-patent-office.webp`, `assets/v74-prior-art-ossuary.webp`, `assets/v74-impossible-claim-examination.webp`, and `assets/v74-perpetual-license-tribunal.webp`.

**v74 状态：** 已实现，Codex 独立浏览器验收通过（8441 assertions）。修复 P1：sceneInit 主同步链原先只到 v73，未调用 v74 的 sync/paint/replay，导致合法解锁态下 Remembrance 的 v74 入口、记忆、图鉴均未初始化；已补齐 v74 全套同步调用并追加整页初始化链回归测试。四锁定深链回退、修复后 sceneInit 同步链、ready-zero、4 专利覆盖解锁终审/3 专利不解锁、完整 isTrusted 办理链与审查员返回、坏 JSON/数组降级、梦型/后忆 activeExaminer、remembrance 双入口 visible+disabled、桌面 1280×720 与手机 390×844 四张图布局/无横溢/触控尺寸均通过。

## v75 — 末日保修局 / APOCALYPSE WARRANTY OFFICE

After the v74 patent office has granted perpetual licenses to every unfinished machine and collected the three perpetual-license rulings, the machines immediately claim that they are still under warranty after the end of the world. The visitor first picks a causality-defying defect (wear before manufacture, still running after the end, or ghost as the only moving part), then one of three impossible proofs of purchase (receipt from an unbuilt factory, dead-sun warranty seal, or descendant repair memory), and finally one of four reality-breaking remedies (replace reality instead of the part, extend the warranty before birth, declare the defect a feature, or bill the apocalypse), producing 3 × 3 × 4 = 36 **warranty claims** such as *出厂前磨损 / 未建工厂收据 / 替换现实而非零件*. Each completed claim leaves an independent **posthumous adjuster** in the corresponding old scene (threshold, remembrance, or unending-gallery), echoing the full narrative and offering a return to the warranty office. After covering all three defects, three proofs, and four remedies (possible in as few as four representative claims), **Remembrance** reveals **召回整批已经售出的现实 · RECALL EVERY REALITY ALREADY SOLD**, opening the **Universal Recall Yard** (`#universal-recall-yard`) and its three final recall rulings: *把世界从流通中召回 · RECALL THE WORLD FROM CIRCULATION*, *给末日安装备用黎明 · INSTALL A SPARE DAWN IN THE APOCALYPSE*, and *因现实擅自维修而作废 · VOID THE WARRANTY FOR REALITY'S SELF-MODIFICATION*. v75 adds four new scenes (`#apocalypse-warranty-office`, `#proof-of-purchase-morgue`, `#post-world-repair-bench`, `#universal-recall-yard`), bringing the total from 129 to 133, and 39 new codex cells (36 warranty claims + 3 recall outcomes) after the v74 codex. All v75 runtime state lives in the single fault-tolerant key `goddead_v75_apocalypse_warranty` (version 75) with a canonical eleven-field projection; `apocalypseWarrantyOfficeUnlocked` reads only the v74 `getTombstonePatentOffice()` state and v75 never writes the v74 key or any earlier state key. Assets are `assets/v75-apocalypse-warranty-office.webp`, `assets/v75-proof-of-purchase-morgue.webp`, `assets/v75-post-world-repair-bench.webp`, and `assets/v75-universal-recall-yard.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`).

**v75 状态：** 已实现，Codex 独立浏览器验收通过（9051 assertions）。P1 修复：Codex 真实浏览器验收曾发现末日保修局按钮 visible/enabled 但真实/语义点击无反馈；根因为 `chooseApocalypseWarrantyDefect/Proof/Remedy/RecallAction` 与 `lock*`、`sync*` 查找的 ID 前缀（`defect-` / `proof-` / `remedy-` / `recall-action-`）与 `index.html` 真实 ID（`apocalypse-warranty-defect-` / `proof-of-purchase-` / `post-world-repair-remedy-` / `universal-recall-`）不一致，导致 `buttonAvailable` 与事件监听绑定到不存在元素；已统一改为真实 DOM ID，并新增 handler ID 与 DOM 存在性对查回归。静态 `node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check` 全绿，`node tests/site.test.mjs = 9051 assertions passed`。真实 Chrome 链路：Remembrance 入口 → `worn-before-manufacture` → `receipt-from-unbuilt-factory` → `replace-reality-not-part` → threshold 空厂理赔员 → 返回保修局；覆盖种子后 real mouse 召回世界 → threshold 通过。桌面四幕与移动 390×844 保修局视觉可读、无横向溢出；页面自身 `console.warn/error = 0`（Immersive Translate 扩展自身两条超时错误为第三方扩展噪声，明确排除）。locked/ready-zero/39 格图鉴/坏 JSON/七类 pending/三处 activeAdjuster/coverage/幂等/三轴矩阵由 9051 静态与浏览器矩阵覆盖。

## v76 — 现实退款处 / REALITY REFUND COUNTER

After the v75 recall yard has voided reality's warranty for self-modification, everything that still exists begins suing for a refund of the price it paid to be sold. The visitor first picks a wrongly-sold existence (body bought with childhood, name paid with forgetting, or years leased from death), then one of three proofs of payment (childhood price tag, erased-name receipt, or death-issued refund reason), and finally one of four reality refund remedies (refund to nonexistence, restore the original absence, exchange for a possible self, or charge reality a restocking fee), producing 3 × 3 × 4 = 36 **reality refund cases** such as *用童年购买的肉身 / 童年价签 / 原路退回不存在*. Each completed case leaves an independent **posthumous cashier** in the corresponding old scene (borrowed-childhood, blank-name-cloakroom, or lifetime-pawn-vault), echoing the full narrative and offering a return to the refund counter. After covering all three subjects, three proofs, and four remedies (possible in as few as four representative cases), **Remembrance** reveals **起诉现实从未符合描述 · SUE REALITY FOR NEVER MATCHING ITS DESCRIPTION**, opening the **Class-Action Court** (`#class-action-court`) and its three final refund rulings: *退回全部存在并全额退款 · RETURN EXISTENCE FOR A FULL REFUND*, *把所有肉身退回童年 · REFUND EVERY BODY TO CHILDHOOD*, and *判现实虚假宣传 · CONVICT REALITY OF FALSE ADVERTISING*. v76 adds four new scenes (`#reality-refund-counter`, `#proof-of-existence-incinerator`, `#reality-return-inspection`, `#class-action-court`), bringing the total from 133 to 137, and 39 new codex cells (36 refund cases + 3 class-action outcomes) after the v75 codex. All v76 runtime state lives in the single fault-tolerant key `goddead_v76_reality_refund` (version 76) with a canonical eleven-field projection; `realityRefundCounterUnlocked` reads only the v75 `getApocalypseWarrantyOffice()` state and v76 never writes the v75 key or any earlier state key. Assets are `assets/v76-reality-refund-counter.webp`, `assets/v76-proof-of-existence-incinerator.webp`, `assets/v76-reality-return-inspection.webp`, and `assets/v76-class-action-court.webp` (runtime WebP 1536×1024 produced from source PNGs with Pillow q85 method=6; all four source PNGs are native 1536×1024; sha256 frozen in `tests/site.test.mjs`).

**v76 状态：** 已实现，当前整库静态测试 `node tests/site.test.mjs = 10277 assertions passed`。Chrome 深链种子真实点击已确认 Remembrance 记忆、图鉴与普通入口正确显露；完整三段链、三处退款员回流、集体诉讼与桌面/移动几何矩阵仍待独立验收。

## v77 — 自我真伪鉴定所 / AUTHENTICITY OFFICE OF THE SELF

After the v76 class-action court has convicted reality of false advertising, every possible self appears with the same exchange slip. The visitor first picks a self that demands to be recognized as original (original personality, replacement memory, or counterfeit soul), then one of three provenance seals (first-wound seal, childhood-mirror testimony, or warm death-mask cast), and finally one of four authenticity methods (certify the earliest version, compare memories against scars, let the copy identify the original, or declare authenticity transferable), producing 3 × 3 × 4 = 36 **self-authenticity certificates** such as *原装人格 / 第一伤口封印 / 认证最早版本*. Each completed certificate leaves an independent **posthumous authenticator** in the corresponding old scene (scar-loom, borrowed-childhood, or lifetime-pawn-vault), echoing the full narrative and offering a return to the authenticity office. After covering all three claimants, three provenances, and four methods (possible in as few as four representative certificates), **Remembrance** reveals **裁定哪一个自己有权继续说“我” · DECIDE WHICH SELF MAY KEEP SAYING I**, opening the **Final Tribunal of Identical Selves** (`#final-authenticity-tribunal`) and its three final authenticity rulings: *只承认一个原装 · RECOGNIZE A SINGLE ORIGINAL*, *合并所有可能的自己 · MERGE EVERY POSSIBLE SELF*, and *让每个复制品成为原装 · MAKE EVERY COPY AN ORIGINAL*. v77 adds four new scenes (`#self-authenticity-office`, `#self-provenance-vault`, `#soul-counterfeit-examination`, `#final-authenticity-tribunal`), bringing the total from 137 to 141, and 39 new codex cells (36 certificates + 3 tribunal outcomes) after the v76 codex. All v77 runtime state lives in the single fault-tolerant key `goddead_v77_self_authenticity` (version 77) with a canonical eleven-field projection; `selfAuthenticityOfficeUnlocked` reads only the v76 `getRealityRefund()` state and v77 never writes the v76 key or any earlier state key. Assets are `assets/v77-authenticity-office-of-self.webp`, `assets/v77-self-provenance-vault.webp`, `assets/v77-soul-counterfeit-examination.webp`, and `assets/v77-final-authenticity-tribunal.webp` (all 1536×1024).

**v77 状态：** 已实现，静态测试 `node tests/site.test.mjs = 10277 assertions passed`。`node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check` 全绿。Chrome 深链种子真实点击已确认解锁后的 Remembrance 记忆、39 格图鉴与普通入口显露，完整三段链、三处鉴定员回流、终审与桌面/移动几何矩阵仍待独立验收。

## v78 — 第一人称配给署 / FIRST-PERSON PRONOUN RATIONING BUREAU

After the v77 tribunal has granted authenticity to every copy and collected the three authenticity rulings, the world is crowded with too many simultaneously legitimate instances of "I". The visitor first picks a claimant competing for the first person (many selves in one body, copies sharing one voice, or ownerless silence), then one of three voice entitlement tokens (first-breath token, ownerless signature impression, or future-inherited echo), and finally one of four rationing schemes (ration one "I" per breath, lend the voice to the shadow, rotate one voice among all copies, or let silence claim the pronoun), producing 3 × 3 × 4 = 36 **first-person rationing orders** such as *多我 / 首息 / 一息一我*. Each completed order leaves an independent **posthumous voice allocator** in the corresponding old scene (threshold, blank-name-cloakroom, or remembrance), echoing the full narrative and offering a return to the rationing bureau. After covering all three speakers, three entitlements, and four schemes (possible in as few as four representative orders), **Remembrance** reveals **裁定谁拥有最后一个第一人称 · DECIDE WHO OWNS THE LAST FIRST PERSON**, opening the **Final Court of Ownerless Voices** (`#ownerless-voices-court`) and its three final vocal rulings: *让所有肉身轮流拥有一声 · GRANT ONE VOICE TO ALL IN TURN*, *废除第一人称配给 · ABOLISH FIRST-PERSON RATIONING*, and *只承认沉默有权发声 · RECOGNIZE SILENCE AS THE ONLY SPEAKER*. v78 adds four new scenes (`#first-person-rationing-bureau`, `#voice-entitlement-archive`, `#pronoun-allocation-chamber`, `#ownerless-voices-court`), bringing the total from 141 to 145, and 39 new codex cells (36 orders + 3 court outcomes) after the v77 codex. All v78 runtime state lives in the single fault-tolerant key `goddead_v78_first_person_rationing` (version 78) with a canonical eleven-field projection; `firstPersonRationingUnlocked` reads only the v77 `getSelfAuthenticity()` state and v78 never writes the v77 key or any earlier state key. Assets are `assets/v78-first-person-rationing-bureau.webp`, `assets/v78-voice-entitlement-archive.webp`, `assets/v78-pronoun-allocation-chamber.webp`, and `assets/v78-ownerless-voices-court.webp` (all 1536×1024).

**v78 状态：** 已由 Gemini 3.7 Flash High 完成实现并提供获采纳的 v70–v78 解锁链线性化修复逻辑，Codex 完成性能根因诊断、按现有上下文集成与独立验收。静态门禁全绿（`node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check`，`node tests/site.test.mjs` 恰好 10954 assertions passed，含永久线性化回归）。无扩展 In-App Browser 的成熟 v64–v77 存档可完成 1280×720 冷启动；真实 Chrome 已走通首息路线的 `主体 → 凭证 → 方案 → 旧场景发声员 → 返回配给署`，另外两处旧落点的专属反馈与可用返回控件也已核对；四份代表配给可解锁终审，中央「废除第一人称配给」真实裁定后保持 4/36、1/3 与 5 格图鉴，刷新幂等，锁定 hash、坏 JSON 与 console 均通过。三种终审均有静态覆盖，但浏览器只真点中央结局；390×844 手动视口未生效，仅保留窄屏静态断言。本轮未执行 commit、push、deploy 或 v79+ 接线。

## v79 — 未言人格继承院 / COURT OF UNSPOKEN PERSONHOOD

After silence becomes the only legal speaker in v78 and collects the three vocal rulings, every unspoken sentence becomes a personless refugee demanding to inherit names, unlived years, and the right to be answered. The visitor first picks a silent claimant (unsaid love confession, unread final testament, or swallowed cry for help), then one of three intent evidence seals (closed-lip pressure seal, unsigned witness echo, or breath returned from an empty receiver), and finally one of four inheritance modes (inherit the speaker's name, inherit the unlived years, inherit the right to be answered, or refuse a body and live as absence), producing 3 × 3 × 4 = 36 **unspoken personhood grants** such as *未爱 / 闭唇 / 姓名*. Each completed grant leaves an independent **silent executor** in the corresponding old scene (confession, testament-clearing-vault, or unseated-listening-booth), echoing the full narrative and offering a return to the court. After covering all three claimants, three evidence items, and four modes (possible in as few as four representative grants), **Remembrance** reveals **审理所有没能出口的人生 · TRY EVERY LIFE THAT NEVER LEFT THE MOUTH**, opening the **Final Tribunal of Unuttered Estates** (`#unuttered-estate-tribunal`) and its three final inheritance rulings: *把完整一生判给未言之句 · GRANT A WHOLE LIFE TO THE UNSAID*, *把人格分给所有未听见者 · DIVIDE PERSONHOOD AMONG ALL WHO DID NOT HEAR*, and *让说话者成为最后沉默的遗产 · MAKE THE SPEAKER THE ESTATE OF LAST SILENCE*. v79 adds four new scenes (`#unspoken-personhood-court`, `#silent-intent-archive`, `#personhood-inheritance-examination`, `#unuttered-estate-tribunal`), bringing the total from 145 to 149, and 39 new codex cells (36 grants + 3 tribunal outcomes) after the v78 codex. All v79 runtime state lives in the single fault-tolerant key `goddead_v79_unspoken_personhood` (version 79) with a canonical eleven-field projection; `unspokenPersonhoodCourtUnlocked` reads only the v78 `getFirstPersonRationing()` state and v79 never writes the v78 key or any earlier state key. Assets are `assets/v79-unspoken-personhood-court.webp`, `assets/v79-silent-intent-archive.webp`, `assets/v79-personhood-inheritance-examination.webp`, and `assets/v79-unuttered-estate-tribunal.webp` (all 1536×1024).

**v79 状态：** Gemini 3.7 Flash High 编写生产前端、测试和两项修复；Codex 负责设计素材、应用输出、诊断与独立 QA。四项静态门禁全绿，最终 `11535 assertions passed`。真实 4 条三段点击流合计覆盖 3 claimant / 3 evidence / 4 mode；三处执行官回程、4/36 终审门槛、中央动作/结局、4 grants / 1 tribunal outcome 的重载严格相等与 UI 5 格图鉴均通过。修复运行时 `_v78unlocked` / 持久化十一键分离，以及 v29 `!unspokenPersonhoodBridgeAllows(target)` 窄桥，避免 `confession` 被改写到 `#corridor`。桌面 1280×720、移动 390×844、四图 1536×1024、热区与横溢、锁定/malformed、console/page/resource 0 均通过。证据：`design-qa-evidence/v79-browser-qa.json`、`design-qa-evidence/v79-executor-confession-desktop.png`、`design-qa-evidence/v79-unspoken-personhood-court-desktop.png`、`design-qa-evidence/v79-tribunal-coverage-desktop.png`、`design-qa-evidence/v79-unspoken-personhood-court-mobile.png`。仅本地验收，未 commit、push、deploy 或发布。

## v80 — Asylum for Unfinished Thoughts / 未遂思想收容所

After v79 grants personhood to every unspoken sentence and records all three estate rulings, the sentences that still never reached a conclusion demand medical rather than legal custody. The visitor first admits one of three unfinished thoughts, then identifies one of three interruption traces, and finally chooses one of four counterfactual therapies, producing 3 × 3 × 4 = 36 **thought admissions**. Each completed admission leaves an independent physician in the matching old scene (`reverse-stairwell`, `blank-name-cloakroom`, or `unlived-nursery`) and offers a return to the asylum. Covering all three thoughts, three traces, and four therapies opens the **Last Conclusion Hearing** with three outcomes: `every-unfinished-thought-kept-living`, `the-thought-completed-its-thinker`, and `all-abandoned-possibilities-were-recycled`.

v80 adds four scenes (`#unfinished-thought-asylum`, `#interruption-trace-archive`, `#counterfactual-treatment-lab`, `#last-conclusion-hearing`), bringing the total from 149 to 153, plus 39 codex cells (36 admissions + 3 hearing outcomes). Its only persistent key is `goddead_v80_unfinished_thought_asylum`, projected to the canonical eleven fields `version / visited / draft / admissions / hearingOutcomes / admissionRuns / hearingRuns / thoughtTallies / lastOutcome / activePhysician / pending`. The four runtime WebP assets are `assets/v80-unfinished-thought-asylum.webp`, `assets/v80-interruption-trace-archive.webp`, `assets/v80-counterfactual-treatment-lab.webp`, and `assets/v80-last-conclusion-hearing.webp` (natural 1536×1024).

**v80 状态：** Gemini 3.7 Flash High 编写生产前端、测试、缺陷修复与实现文档；Codex 负责设计、生图、应用输出、诊断、真实浏览器 QA 与证据。两个 `node --check`、`git diff --check` 全绿，`node tests/site.test.mjs` 输出 `site.test.mjs: 12213 assertions passed`。Computer Use 在同一个 Chrome 窗口 / 标签页完成代表 admission 闭环、cold source / target pending、四幕桌面看图、asylum / hearing 两幕移动看图，以及 `#unending-gallery` / `#counterfactual-spindle` 两条 hearing 真点击；第三条 hearing 只由自动矩阵覆盖。移动 viewport 500×778、documentWidth 500、无横向溢出。证据见 `design-qa-evidence/v80-browser-qa.json` 与四张 v80 桌面 / 移动截图。本轮仅本地验收，未 commit、push、deploy 或发布。

## v81 — 后悔回收厂 / REGRET RECLAMATION PLANT

- **场景扩充**：场景规模由 153 增至 157（新增 4 个场景：`regret-reclamation-plant` 后悔回收厂、`abandonment-residue-weighhouse` 遗弃残渣称重房、`second-life-smelting-line` 二世冶炼线、`zero-waste-life-furnace` 零废弃人生熔炉）。
- **视觉资源**：交付 4 组 1536×1024 高清源图 PNG 与运行时 WebP，均通过尺寸与 SHA-256 哈希校验。
- **重塑与熔炼矩阵**：支持 3×3×4=36 批次重塑组合、3 种熔炉产物，共计 39 个典籍图鉴单元格。
- **跨场景调度**：重塑回收器支持跳转至 `descending-appeals-stair`、`borrowed-childhood`、`identity-correction`；人生熔炉支持注入产物至 `remembrance`、`unending-gallery`、`offering`。
- **状态与防御**：统一采用单一持久化键 `goddead_v81_regret_reclamation`，强校验 11 项规范字段（`version`, `visited`, `draft`, `batches`, `furnaceOutcomes`, `batchRuns`, `furnaceRuns`, `materialTallies`, `lastOutcome`, `activeReclaimer`, `pending`）；7 种严格 pending 校验类型；全站配置恰好 18 处 `isTrusted` 原生点击守卫，杜绝脚本伪造操作。
- **质量门禁**：通过 Node 语法检查与 git diff 检查；`site.test.mjs: 12967 assertions passed`；在单 Chrome 窗口/标签页中通过 Computer Use 完成人工 4 批次验证，覆盖 3 种材料、3 种残渣、4 种用途及全部三个熔炉目标 `remembrance`、`unending-gallery`、`offering`；完成 figure role="img" 可访问性树修复；生成证据路径 `design-qa-evidence/v81-browser-qa.json`、`design-qa-evidence/v81-regret-reclamation-plant-desktop.png` 与 `design-qa-evidence/v81-regret-reclamation-plant-mobile.png`；全部变更保持本地未提交状态，未执行 commit/push/deploy/publication。

## v83 — 伤害考古局 / BUREAU OF HARM ARCHAEOLOGY

- **路线标识**: `harm-archaeology-bureau`, `forensic-mercy-excavation`, `crime-scene-without-offender`, `second-harm-hearing-court`
- **场景规模**: 4 个新场景（总场景数 165），视觉资产尺寸 1536x1024（缓存标记 `v=83`）
- **机制设计**: 3 大遗址 (`closed-scar-site`, `zeroed-ledger-site`, `innocent-soil-site`) × 3 种法医器具 (`pain-reconstruction-brush`, `responsibility-pollen-sieve`, `missing-witness-cast`) × 4 种案发阐释 (`the-offender-never-existed`, `excavation-created-the-truth`, `the-wound-was-an-alibi`, `the-archive-was-the-second-offender`)，产出 36 份伤害考古报告并解锁二次伤害听证庭（3 种终局裁决）。
- **状态持久化**: 本地存储键名 `goddead_v83_harm_archaeology`，严格包含 11 个标准字段，18 处交互事件均绑定 `isTrusted` 校验，未解锁或非法直接访问重定向至 `#remembrance`。
- **验收凭证**: 自动化测试全部通过（14319 个断言），单窗口桌面与移动端真实浏览器 QA 验收通过（凭证见 `design-qa-evidence/v83-browser-qa.json` 及截图）。

## v82 — 宽恕填埋场 / FORGIVENESS LANDFILL
- 新增 4 处场景（157 -> 161）：`forgiveness-landfill`、`inert-harm-certificate-vault`、`mercy-burial-trench`、`harmlessness-final-well`。
- 4 张冻结源 PNG 与 4 张运行时 WebP（自然分辨率 1536x1024），静态缓存查询版本升至 `v=82`。
- 废料 (3) × 凭证 (3) × 处置 (4) 组合构成 36 种填埋记录，配合 3 种井裁结局，共 39 格图鉴单元。
- 独立持久化存储键 `goddead_v82_forgiveness_landfill`（包含 11 个规范字段、7 种挂起类型与 3 族回写），解锁严格单向读取 v81 且全局重置不写入 v81。
- 接入 18 处 `isTrusted` 点击监听与 3 族记录器回跳目标，完成真实浏览器全链路交互与异常数据恢复验收。

## v86 存在放弃登记局 / REGISTRY FOR RENOUNCING EXISTENCE (2026-08-30)

- **场景与资产**：场景总数 173→177；新增 `existence-renunciation-registry`、`proof-of-nonexistence-archive`、`ontological-disinheritance-chamber`、`civil-nonexistence-final-tribunal` 四个 route；配齐 1536×1024 源 PNG 与运行时 WebP，静态缓存标记 `v=86`。
- **状态契约**：状态键 `goddead_v86_existence_renunciation` 规范包含 11 个字段、7 处 strict pending 校验与 18 项 `isTrusted` 交互事件；生成 36 条本体注销令与 3 种终庭裁定；只读校验 v85 解锁状态且绝不回写旧版本键。
- **落点与回程**：3 个旧落点 `birth-ballot-booth`、`blank-name-cloakroom`、`reality-refund-counter`；空摇篮登记员、预先抹除登记员与未交付肉身登记员均支持受信任交互返回。
- **自动化与人工 QA**：自动化套件全覆盖 36 本体注销令与 3 裁定；人工验收通过 4 条受信任流程合计覆盖 3/3 renunciants / 3/3 evidences / 4/4 clauses / 3/3 old targets，且三终庭裁定真实导航抵达 `threshold`、`remembrance` 与 `unending-gallery`。
- **静态门禁与容灾**：门禁通过 `node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check` 及 `node tests/site.test.mjs`（`site.test.mjs: 14713 assertions passed`）。
- **环境与分工**：单窗单标签运行，Desktop 与 Mobile（390×844，`scrollWidth`/`bodyScrollWidth` 为 390）均无横向溢出；未解锁直达回退 `threshold`，刷新恢复 `unending-gallery`，坏 JSON 安全容错无崩溃并保留原始值直至测试还原，精确恢复原 22 个 localStorage 键（包含原始 v83 听证数 1，无 QA 残留键）；归档 1 份 JSON 证据与 5 张截图；本地实装与独立验收完成，未执行 commit、push、deploy 或 public release。分工由 Gemini 编写生产前端、测试与实现文档，Codex 负责设计、资产生成接入、补丁应用与 Computer Use QA 验收。

### v84 无罪证人保护院 / WITNESS PROTECTION FOR THE INNOCENT (2026-08-29)

- **新增场景**：`innocent-witness-protection`（无罪证人保护院）、`identity-causality-laundry`（身份因果洗衣房）、`memory-relocation-safehouse`（记忆迁移安全屋）、`anonymous-truth-lifetime-court`（匿名真相终身安置庭），场景总数达 169（Cache `v=84`）。
- **核心机制**：承接 v83 伤害考古局全部 3 项听证结果解锁；提供 3 名无罪证人 × 3 种洗消程序 × 4 条保护条款（共 36 种安置组合矩阵）、3 位身份掩护员（调度 `blank-name-cloakroom`、`borrowed-shadow-gallery`、`unreturned-witness-gallery`）以及终身安置庭 3 种历史裁决终局。
- **数据架构**：本地存储键 `goddead_v84_innocent_witness_protection`，规范定义 11 个持久化字段（`version`, `visited`, `draft`, `placements`, `courtOutcomes`, `placementRuns`, `courtRuns`, `witnessTallies`, `lastOutcome`, `activeHandler`, `pending`），严格单向读取 `goddead_v83_harm_archaeology`，重置操作保持隔离。
- **验收与交付**：全量测试套件 14,473 项断言全部通过；在单一 Chrome 窗口且单一标签页下完成实机路由门禁、交互分支、容灾及双端视觉热区验收；交付状态严格保持本地闭环（no commit / no push / no deploy / no public release）。

## v85 孤事实认领处 / CLAIM OFFICE FOR ORPHANED FACTS (2026-08-30)

- **场景与资产**：场景总数 169→173；新增 `orphaned-fact-claim-office`、`fact-inheritance-vault`、`causal-estate-execution-desk`、`ownerless-truth-estate-court` 四个 route；配齐 1536×1024 源 PNG 与运行时 WebP，静态缓存标记 `v=85`。
- **状态契约**：状态键 `goddead_v85_orphaned_fact_claims` 规范包含 11 个字段、7 处 strict pending 校验与 18 项 `isTrusted` 交互事件；生成 36 条继承契与 3 种终庭裁定；只读校验 v84 解锁状态且绝不回写旧版本键。
- **落点与回程**：3 个旧落点 `contradictory-evidence-archive`、`blank-name-cloakroom`、`minute-before-archive`；伤印执行员、影供执行员与未来账单执行员均支持受信任交互返回。
- **自动化与人工 QA**：自动化套件全覆盖 36 继承契与 3 裁定；人工验收通过 4 条受信任流程合计覆盖 3 facts / 3 proofs / 4 obligations / 3 old targets，且三终庭真实导航抵达 `threshold`、`remembrance` 与 `unending-gallery`。
- **缺陷修复**：针对 Codex 诊断出的第三裁定被旧 guard 改写问题，Gemini 增加 `!orphanedFactBridgeAllows('unending-gallery')` 放行并补齐永久回归断言；门禁通过 `node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check` 及 `node tests/site.test.mjs`（`site.test.mjs: 14593 assertions passed`）。
- **环境与分工**：单窗单标签运行，Desktop 1312×768 与 Mobile 390×844 均无横向溢出；验证刷新幂等、坏 JSON 安全回退并恢复原 22 个 localStorage 键；产出 1 份 JSON 证据与 5 张截图；本地实装完成，未执行 commit、push、deploy 或 public release。分工由 Gemini 编写生产前端、测试、修复与实现文档，Codex 负责设计、资产生成接入、机械应用、诊断与 Computer Use QA 验收。

## Version design documents

- `docs/V84WitnessProtectionForInnocentDesign.md`: v84 无罪证人保护院 / WITNESS PROTECTION FOR THE INNOCENT；本地实装与独立验收已完成。
- `docs/V85OrphanedFactClaimOfficeDesign.md`: v85 孤事实认领处 / CLAIM OFFICE FOR ORPHANED FACTS；本地实装与独立验收已完成。
- `docs/V86ExistenceRenunciationRegistryDesign.md` 至 `docs/V90CauselessConsequenceRefugeeAuthorityDesign.md`：v86-v90 的冻结设计、状态合同、素材清单与验收边界。

## v88 未发生事件拍卖行 / AUCTION HOUSE FOR EVENTS THAT NEVER HAPPENED (2026-08-30)

- **场景与资产**：场景总数 181→185；新增 `auction-house-for-events-that-never-happened`、`catalogue-of-unoccupied-reality`、`counterfactual-bidding-floor`、`retroactive-occurrence-title-court`；缓存标记升至 `v=88`。配齐 4 张源 PNG 与 4 张运行时 WebP，全部 1536×1024，冻结明细见 `docs/V88UnhappenedEventAuctionHouseDesign.md`。
- **机制与状态**：3 bidders × 3 lots × 4 methods = 36 purchases，并提供 3 项 title outcomes；coverage 最少 4 份且覆盖 3/3 bidders、3/3 lots、4/4 methods。状态键 `goddead_v88_unhappened_event_auction` 严格投影 11 个顶层字段，校验 7 类 pending，绑定恰好 18 个 `isTrusted` 点击监听；v88 只读 v87，不回写旧键。
- **旧场景与终局**：三位 auctioneer 分别在 `forgiveness-landfill`、`undeclared-war-room`、`unlived-nursery` 提供窄桥返回；三项产权裁定分别抵达 `threshold`、`remembrance`、`unending-gallery`。
- **自动化门禁**：`node --check script.js`、`node --check tests/site.test.mjs`、`git diff --check` 与 `node tests/site.test.mjs` 全绿，最终输出 `site.test.mjs: 14992 assertions passed`。
- **Computer Use QA**：在一个 Chrome 窗口、一个标签页中完成 4 条真实成交并覆盖 3/3/4，验证三回桥、三终局、刷新幂等、malformed JSON 回退与干净控制台；桌面 915×774 与手机 390×844 均无横向溢出，热点大于 44×44。compositor 空白截图未保存，证据见 `design-qa-evidence/v88-browser-qa.json`。
- **恢复与分工**：QA 后恢复用户原 22 个 localStorage 项和 `#remembrance`，清除临时键并关闭 DevTools 与设备模拟。Gemini 3.7 Flash High 编写生产前端、测试、修复与实现文档；Codex 负责设计、ImageGen 生图、机械集成、诊断和独立 QA。
- **本地边界与后续**：本轮未 commit、push、deploy 或 public release。下一版本为 `既成事实拆迁局 / EVICTION AUTHORITY FOR ACCOMPLISHED FACTS`；v88 不包含 v89 实现。

### v89: 既成事实拆迁局 / EVICTION AUTHORITY FOR ACCOMPLISHED FACTS (2026-08-30)
- 新增第 186 至 189 处场景（全站总场景数增至 189）：
  - `accomplished-fact-eviction-authority`（既成事实拆迁局）
  - `condemned-history-survey-office`（危历史勘测所）
  - `retroactive-demolition-yard`（追溯拆除场）
  - `final-occupancy-appeal-court`（最后居住权上诉庭）
- 状态持久化：`goddead_v89_accomplished_fact_eviction`，包含严格规范的 11 个规范键。
- 内容矩阵：3 类租客 × 3 处危历史产权 × 4 种拆除方式 = 36 道腾退令；3 种上诉判决；39 格典籍档案。
- 旧场景联动：在 `birth-ballot-booth`（出生投票间）、`crime-scene-without-offender`（无加害者犯罪现场）、`undeclared-war-room`（未宣战室）部署拆迁勘测员/法警；目录与纪念碑接入；静态资源缓存版本升级至 `?v=89`。
- 资产生成：
  - `assets/v89-accomplished-fact-eviction-authority.webp`
  - `assets/v89-condemned-history-survey-office.webp`
  - `assets/v89-retroactive-demolition-yard.webp`
  - `assets/v89-final-occupancy-appeal-court.webp`
- 交互与安全：18 处受信任点击监听器配备严格 `event.isTrusted` 守卫。
- 自动化验证：`node tests/site.test.mjs` 通过 15657 项断言（较 v88 增加 665 项）。
- 本地可见 Chrome 真机验收：单窗口单标签页（DevTools 仅同窗停靠）完成完整流程与 390×687 移动端适配验收。当前处于本地完成阶段，未执行 commit、push 或部署。

## v90 无因后果难民署 / REFUGEE AUTHORITY FOR CONSEQUENCES WITHOUT CAUSES (2026-08-31)

- **场景与资产**：场景总数 189→193；新增 `causeless-consequence-refugee-authority`、`borrowed-cause-sponsorship-office`、`causal-border-processing-station`、`final-asylum-tribunal-for-causeless-consequences`，缓存标记升至 `v=90`；4 组源 PNG 与运行时 WebP 均为 1536×1024。
- **玩法矩阵**：3 类无因难民 × 3 名借因担保人 × 4 种边境程序 = 36 份庇护案；覆盖 3/3/4 后开放三项终审裁定，共 39 格图鉴。
- **旧场景回桥**：门槛认生、记忆代伤、空框认战分别将流程送往 `threshold`、`remembrance`、`unending-gallery`，由本章领事受信任点击返回难民署。
- **状态与门禁**：独立键 `goddead_v90_causeless_consequence_refugee` 保持 11 个规范字段、7 类严格 pending、18 处 `isTrusted` 点击守卫；v90 只读 v89，非法深链回退 `remembrance`。
- **当前验证**：两项 `node --check`、`git diff --check` 和全量 `node tests/site.test.mjs` 通过，输出 `site.test.mjs: 16343 assertions passed`；本地浏览器复核确认页面初始化、锁定深链归一到 `#remembrance`、刷新稳定且控制台无 error/warn。完整 3/3/4 受信任点击矩阵沿用实现期测试合同，本次整理未重写生产逻辑。
